---
phase: 05-camada-2-web-desktop-e-profundidade
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/dados/observatorio.ts
  - src/componentes/observatorio.tsx
  - src/app/(bastidor)/observatorio/page.tsx
  - src/estilos/observatorio.css
autonomous: true
requirements: [WEB-07]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O painel de procedência é tela de primeira classe, com os números reais do acervo: quanto é ic, quanto é derivado, quanto é autorado — em entidades E em arestas (D-88)"
    - "Os indicadores de impacto cultural são calculados na hora a partir do grafo e do repertório das personas, e cada um mostra de onde saiu (D-87)"
    - "Onde o dado não sustenta um indicador, ele aparece declarando a ausência COM o denominador — nunca zerado em silêncio (D-90)"
    - "O seletor de público troca o recorte e não a tela: a URL não muda e os blocos são os mesmos (D-89)"
    - "O mapa de desertos culturais está na tela, com a frase que não pode ser suavizada"
  artifacts:
    - src/dados/observatorio.ts
    - src/componentes/observatorio.tsx
    - src/estilos/observatorio.css
  key_links:
    - "observatorio.ts → contagens() de grafo.ts e meta.json — os números de procedência saem da contagem, nunca de literal"
    - "observatorio.ts → repertorioDe() de repertorio.ts para as 3 personas — é o que sustenta ampliação de repertório"
    - "observatorio.tsx → CamadaDesertos/LeituraDesertos de desertos.tsx e densidadePorUf() de geo.ts, sem editar nenhum dos dois"
---

<objective>
O Observatório existe: indicadores de impacto cultural que não são pageview, e o painel de
procedência do acervo como tela de primeira classe.

Purpose: D-88 é, junto com o mapa de desertos, a tela que mais distingue esta proposta.
Nenhum concorrente vai mostrar quanto do próprio protótipo é inventado — e é exatamente isso
que torna o resto crível. O painel de procedência é o princípio 9 virando interface: ele diz,
com número, que 4.826 entidades vieram do Itaú Cultural, 2.937 foram derivadas por nós e 47
são autoradas, e a partir daí tudo o mais que a tela afirma passa a ser verificável.

Output: `/observatorio/` preenchida, na rota que já existe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/5-CONTEXT.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/05-01-SUMMARY.md
@.planning/phases/03-camada-1-agenda-territorio-e-busca/03-03-SUMMARY.md
@src/componentes/desertos.tsx
@src/app/(app)/mapa/page.tsx
@src/dados/repertorio.ts
@src/dados/personas.ts
@src/dados/geo.ts
@src/estilos/web.css
</context>

<contrato_herdado>
**Leia `05-01-SUMMARY.md` antes de escrever uma linha.**

1. **`src/estilos/observatorio.css` já existe, vazia de regra.** É sua, e é a única folha deste
   plano. **Não toque em `src/app/globals.css`**, nem em `src/estilos/web.css`, nem em
   `src/estilos/mapa.css`.
2. Deste plano são `data-observatorio`, `data-indicador`, `data-publico`,
   `data-procedencia-painel`, `data-procedencia-fatia` e `data-denominador`. Mais
   `data-nao-sustenta`, que a fase 5 inteira reaproveita para D-90.
3. **Orçamento de chunks deste plano: 60 KB.**

`npm run verificar-fase3` está vermelha no gate 8 (`globals.css` ancorado em `c03f627`) desde
o commit de 05-01. Esperado; o reancoramento é de 05-08; **não conserte aqui**. Para regressão
use `verificar-fase2` e `verificar-fase4`.

**Esta rota é de bastidor.** O layout de `(bastidor)` já monta `<AvisoDesktop>` e esconde o
conteúdo na visão app. Não desfaça isso. O bastidor **não monta barra de abas**: «cabe na
primeira vista» mede a moldura inteira, sem descontar os 59 px da barra.

**`src/componentes/desertos.tsx` é da fase 3 e você não pode editá-lo.** Ele exporta
`CamadaDesertos`, `LeituraDesertos` e o tipo `DadosDesertos`, e `src/app/(app)/mapa/page.tsx`
mostra como montar `DadosDesertos` a partir de `densidadePorUf()` — **leia e repita esse
molde no seu `page.tsx`**, não o importe de `mapa/page.tsx`.
</contrato_herdado>

<protocolo_de_disco>
Volume a 96% com histórico de despejo do iCloud — `stat` informa tamanho, a leitura devolve
zero byte. Confirme cada arquivo existente com `wc -c` contra `git show HEAD:<arquivo> | wc -c`
**antes de editar**; divergiu ou leu zero, restaure com `git show HEAD:<arquivo> > <arquivo>`,
nunca com `brctl download`. Antes de cada commit leia `git diff --cached --numstat`; depois
confira os bytes NO GIT e leia `git diff-tree --name-status -r HEAD`. Empurre com
`git push espelho HEAD`. Confira o STATE.md depois de qualquer `state.update-progress`.
</protocolo_de_disco>

<o_que_foi_medido_antes_de_planejar>
Apurado contra `src/dados/gerado/meta.json` e o grafo em disco durante o planejamento.
**Confirme cada número calculando-o — não copie estes literais para dentro do código.** Eles
estão aqui para você saber quando o seu cálculo está errado.

**Procedência de entidade (D-88).** ic **4.826** · derivado **2.937** · autorado **47** ·
total **7.810**. As três somam o total exatamente.

**Procedência de aresta (D-88).** ic **14.882** · derivado **51.600** · autorado **81** ·
total **66.563**. Também fecham. **Mostre as duas leituras**: 0,6% das entidades são autoradas,
mas a proporção de arestas conta outra história, e é a segunda que explica por que o grafo
parece rico.

**Repertório das personas (D-87).** 3 personas. Entidades no repertório: 10 (Maria), 12
(Carlos), 9 (Joana). Linguagens atravessadas: 8, 5, 10. Linguagens no adjacente: 11, 12, 19.
Linguagens declaradas por Maria: 3. **n = 3 é o denominador e ele tem de estar na tela** — um
indicador de impacto sobre 3 pessoas autoradas que não diga «3 pessoas autoradas» é o tipo de
número que a proposta inteira existe para combater.

**Acessibilidade (o caso mais duro de D-90).** Nas 7.810 entidades: `libras` 56 (180 contando
derivadas), `open_caption` 1, `subtitle` 1, e **zero** em `audio_description`,
`descriptive_subtitle`, `closed_caption`, `simultaneous_translation`, `stenotypy`. **5 das 8
dimensões medem zero em todo o acervo.** Ficha de acessibilidade: 5.108 declaram, 2.702 não
declaram — e essa é a distinção de D-43 que separa declarado-ausente de não-declarado.

**Gratuidade.** 0 de 300 eventos declaram ingresso; as 2.425 ocorrências saem todas gratuitas
porque `gratuito` é a negação de um campo que ninguém preenche. **Gratuito × pago não recorta
nada neste acervo** e o indicador tem de dizer isso com os dois denominadores, em vez de
desenhar uma barra de 100%.

**Espaço.** 0 de 2.425 ocorrências declaram espaço.

**Território.** 1.380 entidades têm coordenada resolvível, em 8 classes; `densidadePorUf()`
mede a concentração; SP e RJ somam 59% dos registros; 5 estados têm um registro; Sergipe e
Tocantins têm zero.

**Faixa etária não existe no acervo** — nem no CMS, nem na Enciclopédia, nem nas 7.810
entidades. `src/dados/disposicoes.ts` já registra isso por escrito. Se algum recorte do
Observatório for tentado por faixa etária, ele declara a inexistência; não invente proxy.
</o_que_foi_medido_antes_de_planejar>

<tasks>

<task type="auto">
  <name>Task 1: observatorio.ts — os indicadores calculados na hora, cada um com a sua procedência e o seu denominador</name>
  <files>src/dados/observatorio.ts</files>
  <action>
    Crie `src/dados/observatorio.ts`, módulo de build puro, síncrono e memoizado, no molde de
    `duplicatas.ts` e `ocorrencias-studio.ts`. Importa `grafo.ts`, `geo.ts`, `repertorio.ts` e
    `personas.ts` **por valor**, roda só no build, e nada `"use client"` o importa por valor
    (DP-F).

    **O tipo central é o indicador, e ele carrega a honestidade na estrutura, não na prosa.**
    Defina `Indicador` com, no mínimo: `id`, `rotulo`, `valor: number | null`,
    `unidade`, `denominador: {n: number, do_que: string}`, `sustentado: boolean`,
    `procedenciaDoNumero: string` (de onde ele saiu — a função e a coleção, no molde do
    «ORIGEM DO NÚMERO» que `/roteiro` já usa), e `declaracao: string | null` (a frase de D-90,
    obrigatória quando `sustentado` é falso). **`valor: null` com `sustentado: false` é a
    forma de dizer «o dado não sustenta»; zero com `sustentado: true` significa outra coisa e
    a estrutura não pode confundir as duas.**

    Os cinco indicadores de D-87, cada um calculado:

    - **ampliação de repertório** — linguagens atravessadas contra linguagens declaradas, por
      persona e no conjunto. Denominador: 3 personas autoradas.
    - **descoberta de artista novo** — quantas entidades do adjacente de cada persona são de
      classe `pessoa` ou `coletivo` e não estão no repertório dela. Denominador: o tamanho do
      adjacente.
    - **diversidade de linguagem por região** — linguagens distintas por unidade federativa,
      sobre as entidades com coordenada resolvível. Denominador: as 1.380 com coordenada, e as
      7.810 do acervo ao lado, para a fração ficar legível.
    - **circulação territorial** — a concentração medida por `densidadePorUf()`, com os dois
      maiores estados e a fração que eles somam.
    - **gratuito × pago** — o caso em que o dado não sustenta. `sustentado: false`, `valor:
      null`, e a declaração com os dois denominadores.

    **O painel de procedência (D-88) é a peça central**, e é um tipo próprio, não um
    indicador entre outros. Exporte `painelDeProcedencia()` devolvendo as fatias de entidade e
    as de aresta, cada fatia com `procedencia`, `n`, `fracao` e `exemplo` — o título de uma
    entidade real daquela procedência, para a tela poder mostrar do que se trata em vez de só
    a barra. **Nenhum desses números pode ser literal escrito à mão**: todos saem de
    `contagens()` ou da contagem direta. E acrescente `conferencia`: as fatias têm de somar o
    total, e o módulo **quebra alto** se não somarem — uma fatia que não fecha é o sintoma de
    um `porProcedencia` desatualizado, e é melhor não compilar do que exibir.

    **O seletor de público (D-89).** Exporte `PUBLICOS` — editorial, produto, parceiro,
    institucional — e, para cada um, quais indicadores ele destaca e em que ordem. **Ele troca
    o recorte, não a tela**: os mesmos blocos, outra ênfase. Nenhum público pode esconder o
    painel de procedência: ele é de primeira classe nos quatro.

    Exporte `numerosDoObservatorio()` com tudo que a tela e o gate de 05-08 vão citar.

    **Teto deste módulo: 60 KB de DTO.** Meça e exporte o número. Se a diversidade por região
    ficar gorda, agregue por unidade federativa em vez de por entidade — 27 linhas bastam para
    a leitura e 1.380 não cabem no orçamento.
  </action>
  <verify>
    <automated>node_modules/.bin/tsx -e "
import {indicadores, painelDeProcedencia, PUBLICOS, numerosDoObservatorio} from '@/dados/observatorio';
import {contagens} from '@/dados/grafo';
const ind = indicadores();
if(ind.length < 5) throw new Error('indicadores: '+ind.length);
for(const i of ind as any[]){
  if(i.valor===null && i.sustentado) throw new Error(i.id+': valor null com sustentado true');
  if(!i.sustentado && !i.declaracao) throw new Error(i.id+': nao sustentado sem declaracao');
  if(!i.denominador || typeof i.denominador.n!=='number' || !i.denominador.do_que) throw new Error(i.id+': sem denominador');
  if(!i.procedenciaDoNumero) throw new Error(i.id+': sem procedencia do numero');
}
const grat = (ind as any[]).find(i=>/gratu/i.test(i.id));
if(!grat || grat.sustentado) throw new Error('gratuidade tem de vir NAO sustentada neste acervo');
const p = painelDeProcedencia();
const somaE = p.entidades.reduce((a:number,f:any)=>a+f.n,0);
const somaA = p.arestas.reduce((a:number,f:any)=>a+f.n,0);
if(somaE!==7810) throw new Error('entidades somam '+somaE);
if(somaA!==66563) throw new Error('arestas somam '+somaA);
const ic=p.entidades.find((f:any)=>f.procedencia==='ic');
if(ic.n!==4826) throw new Error('ic='+ic.n);
if(PUBLICOS.length!==4) throw new Error('publicos: '+PUBLICOS.length);
const bytes=JSON.stringify({ind,p}).length; if(bytes>60*1024) throw new Error('DTO '+bytes);
console.log('OK', ind.length+' indicadores', 'entidades '+somaE, 'arestas '+somaA, bytes+' bytes');
console.log(JSON.stringify(numerosDoObservatorio()).slice(0,400));
"</automated>
  </verify>
  <done>O módulo devolve cinco ou mais indicadores, cada um com denominador e procedência do número; gratuidade vem não-sustentada com declaração; as fatias de procedência somam 7.810 e 66.563 exatamente; e o DTO cabe em 60 KB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O painel de procedência como tela de primeira classe</name>
  <files>src/componentes/observatorio.tsx, src/estilos/observatorio.css</files>
  <behavior>
    - `data-procedencia-painel` está presente e o seu retângulo começa acima da base da moldura
      SEM rolar: é a primeira coisa que a tela mostra, não um rodapé.
    - Há seis elementos `data-procedencia-fatia` — três de entidade e três de aresta — e a soma
      dos números exibidos bate com 7.810 e 66.563.
    - As barras de cada fatia têm largura MEDIDA proporcional ao número, e a maior fatia é
      medida mais larga que a menor.
    - Todo o painel continua visível com o modo comentado DESLIGADO.
    - Cada `data-indicador` traz um `data-denominador` com altura maior que zero.
  </behavior>
  <action>
    `observatorio.tsx` recebe o DTO por propriedade; nada aqui alcança `@/dados/grafo` nem
    transitivamente (DP-F).

    **O painel de procedência abre a tela, não a fecha (D-88).** Ele vai no topo, com peso
    visual de primeira ordem — não é legenda cinza, é o argumento. Ele mostra as duas leituras
    lado a lado: entidades e arestas. Cada fatia traz o rótulo, o número com separador de
    milhar, a fração, um exemplo real, e uma barra. Junto delas, a frase que explica o que
    cada procedência quer dizer: `ic` é o que veio do acervo do Itaú Cultural, `derivado` é o
    que nós extraímos dele por regra, `autorado` é o que nós escrevemos. **A frase não pode ser
    suavizada** — «autorado» quer dizer inventado por nós para o protótipo, e é dizer isso em
    voz alta que torna os outros 4.826 críveis.

    **Desenhe os gráficos sem biblioteca externa e sem uma única requisição de rede.** A
    discrição de projeto é sua quanto à forma. Uma advertência de engenharia: `visiveis()` do
    preludio de verificação **falha sobre SVG** — `offsetParent` é nulo em elemento SVG.
    Barras feitas de elementos HTML com largura em porcentagem são mensuráveis pelos gates sem
    ginástica; barras em SVG obrigam o gate a medir por `getBoundingClientRect`. Prefira HTML
    para as barras e deixe o SVG para o mapa, que já é SVG.

    Nenhuma largura de barra pode ser literal: ela sai do número, por `style` calculado. Uma
    barra desenhada à mão que não acompanhe o dado é a mentira mais fácil de cometer nesta
    tela.

    Cada indicador (`data-indicador`) mostra: rótulo, valor, unidade, **denominador visível**
    (`data-denominador`) e a procedência do número. Onde `sustentado` é falso, ele mostra a
    declaração de D-90 com `data-nao-sustenta` — e mostra o denominador do mesmo jeito. **Um
    indicador sem lastro não some e não aparece zerado**: ele aparece dizendo quantos de
    quantos.

    Nada disto entra em `<Comentario>`: o painel, os números e as declarações são produto e
    têm de sobreviver ao modo comentado desligado. Dentro de `<Comentario>` vai só o que fala
    SOBRE o protótipo — a explicação do princípio 9, a citação de decisão.

    Layout de desktop em `observatorio.css`, sob `[data-view="web"]`, consumindo `.web-grade` e
    `.web-painel` de `web.css` sem editá-las.

    **Geometria.** A fase 4 pagou duas vezes por gate verde sobre tela quebrada — um índice
    fora da janela e uma primeira linha coberta. Garanta que o painel de procedência caiba na
    primeira vista e que nenhuma barra transborde o seu contêiner. O gate mede retângulos.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-05.ts procedencia</automated>
  </verify>
  <done>No DOM vivo a 1440×960 na visão web: o painel de procedência está na primeira vista com seis fatias somando 7.810 e 66.563, as barras têm largura proporcional medida, cada indicador mostra o seu denominador, e tudo sobrevive ao modo comentado desligado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: O seletor de público, os cinco indicadores e o mapa de desertos</name>
  <files>src/app/(bastidor)/observatorio/page.tsx</files>
  <behavior>
    - Há quatro controles `data-publico`; trocar de público muda a ordem ou a ênfase dos
      indicadores e NÃO muda a URL.
    - O painel de procedência continua presente nos quatro públicos.
    - O mapa de desertos está na tela, com os dois estados sem registro nomeados e a frase que
      distingue registro no acervo de oferta cultural do estado.
    - O indicador de gratuidade aparece com `data-nao-sustenta`, com os dois denominadores, e
      com altura maior que zero com o modo comentado desligado.
    - Nada da tela corre para fora da janela: `scrollWidth` é igual a `clientWidth`.
  </behavior>
  <action>
    `page.tsx` é de servidor e é ele que toca `observatorio.ts` e `geo.ts`. Monte
    `DadosDesertos` pelo mesmo molde de `src/app/(app)/mapa/page.tsx` — `densidadePorUf()`
    cruzado com `UNIDADES_FEDERATIVAS` de `contorno-brasil.ts`, projetado por `projetar()` e
    `caminhoDe()`. **Leia aquele arquivo e repita o molde; não o importe.** Passe o resultado
    para `<CamadaDesertos>` e `<LeituraDesertos>` de `desertos.tsx`, **sem editar
    `desertos.tsx`**, que é da fase 3.

    **O que a camada de desertos mede, e a frase não pode ser suavizada:** registro no acervo
    carregado do Itaú Cultural, e não oferta cultural do estado. Sergipe tem cultura; o acervo
    é que não a documenta. `desertos.tsx` já carrega essa distinção — não a reescreva mais
    fraca ao encaixar o componente.

    **O seletor de público (D-89).** Quatro controles com `data-publico` e `aria-pressed`.
    Trocar de público troca o RECORTE — a ordem, a ênfase, quais indicadores ganham destaque —
    e não a tela: os mesmos blocos, a mesma URL, o mesmo painel de procedência. É a mesma
    lógica do escopo do curador em D-84, e ela é a resposta ao «como crescer sem reescrever»
    do RFP. Diga isso na tela, dentro de `<Comentario>`.

    Se algum recorte tentar faixa etária, ele **declara que o acervo não tem o campo** — nem no
    CMS, nem na Enciclopédia, nem nas 7.810 entidades — em vez de oferecer um corte que não
    corta. `src/dados/disposicoes.ts` já traz a frase; reaproveite o raciocínio.

    Ponha o link para `/redacao/fila/` e `/studio/duplicatas/` no cabeçalho de bastidor, se o
    layout ainda não os oferecer — as superfícies de bastidor têm de se alcançar entre si, e
    quem vai demonstrar não pode digitar URL ao vivo.

    **Meça o custo antes de commitar.** Se o peso de `out/_next/static/chunks` subir mais de
    60 KB, corte campo do DTO — agregue por unidade federativa, corte o exemplo por fatia para
    um só — e declare o corte na tela em vez de reduzir o que a tela afirma.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-05.ts publico && node -e "
const h=require('fs').readFileSync('out/observatorio/index.html','utf8');
const n=(a)=>(h.match(new RegExp('data-'+a+'=\"','g'))||[]).length;
if(n('procedencia-painel')!==1) throw new Error('data-procedencia-painel='+n('procedencia-painel'));
if(n('procedencia-fatia')!==6) throw new Error('data-procedencia-fatia='+n('procedencia-fatia'));
if(n('publico')!==4) throw new Error('data-publico='+n('publico'));
if(n('indicador')<5) throw new Error('data-indicador='+n('indicador'));
if(n('denominador')<5) throw new Error('data-denominador='+n('denominador'));
if(!/4\.826|4826/.test(h)||!/2\.937|2937/.test(h)) throw new Error('numeros de procedencia ausentes do HTML');
console.log('OK HTML: painel=1 fatias=6 publicos=4 indicadores='+n('indicador'));
" && npm run verificar-fase4 && npm run verificar-comentado</automated>
  </verify>
  <done>Os quatro públicos trocam o recorte sem trocar de URL, o painel de procedência sobrevive aos quatro, o mapa de desertos está na tela com a frase intacta, gratuidade aparece declarada com os dois denominadores, e nada transborda a janela.</done>
</task>

</tasks>

<sonda_do_plano>
As tasks 2 e 3 pedem `scripts/sonda-05-05.ts`, sonda descartável — **apague antes do último
commit** e confirme `git status --short scripts/` vazio. A suíte permanente é de 05-08.

API real de `scripts/navegador.mjs`: `servir({raiz})` de `servir-out.mjs` devolve
`{url, fechar}`; `abrirNavegador()` devolve o `cdp` diretamente; o método é `cdp.navegar(url)`;
console em `cdp.consola`; **`cdp.clicar` recebe uma EXPRESSÃO JavaScript que avalia para o
elemento**, não um seletor CSS; `cdp.avaliar(expressao)` e `naPagina(corpo)` embrulham com o
preludio. **Não existe `Network.*`** — prove rede por
`performance.getEntriesByType('resource')` de dentro da página.

**`visiveis()` falha sobre SVG.** O mapa de desertos é SVG: meça-o por
`getBoundingClientRect()` e por atributo, nunca por `visiveis()`.

Visão web por `localStorage.setItem("agenda-cultural:visao","web")` (string crua, não JSON)
mais recarregar; espere `data-hidratado="sim"`.

**Onde o gate afirmar usabilidade, meça geometria contra o contêiner.** Para «o painel de
procedência é tela de primeira classe e não rodapé», a medida certa é o `top` do retângulo do
painel contra a base da moldura **sem rolar** — presença no DOM não prova nada aqui, e é
exatamente a diferença entre painel e rodapé que D-88 exige. O bastidor não monta barra de
abas, então a moldura inteira é o limite.

Para as barras proporcionais, leia `getBoundingClientRect().width` de cada fatia e compare a
ordem das larguras com a ordem dos números. Uma barra desenhada à mão passaria num gate de
presença e falha neste.

Defina `DIR_CAPTURAS` e guarde fotos **fora do repositório** — do painel de procedência e do
mapa de desertos em especial. São as duas telas que a banca vai olhar mais tempo, e é onde um
número certo com desenho errado não aparece em medição nenhuma.
</sonda_do_plano>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grafo → indicador | todo número exibido é uma afirmação pública sobre o acervo e tem de ser calculado |
| build → cliente | `observatorio.ts` alcança 23 MB de grafo e não pode ser importado por valor do cliente |
| artefato → rede | gráfico com biblioteca externa ou tile remoto quebra a promessa medida de zero requisição |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Spoofing | número de procedência escrito à mão divergindo do acervo | critical | mitigate | fatias saem de `contagens()`; o módulo quebra alto se não somarem 7.810 e 66.563; gate confere as somas |
| T-05-21 | Spoofing | barra desenhada com largura fixa não acompanhando o dado | high | mitigate | largura calculada do número; gate compara a ordem das larguras medidas com a ordem dos números |
| T-05-22 | Repudiation | indicador sem lastro aparecendo zerado em silêncio | critical | mitigate | `valor: null` + `sustentado: false` + `declaracao` obrigatória; gate reprova não-sustentado sem declaração |
| T-05-23 | Repudiation | indicador de 3 personas apresentado sem o denominador | high | mitigate | `denominador` é campo obrigatório do tipo; gate exige `data-denominador` com altura maior que zero |
| T-05-24 | Information disclosure | DTO carregando as 1.380 entidades com coordenada | medium | mitigate | agregação por unidade federativa; teto de 60 KB medido no gate da Task 1 |
| T-05-25 | Denial of service | biblioteca de gráfico trazendo requisição de rede | high | mitigate | proibida no plano; gate de rede herdado exige zero requisição externa |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **este plano não instala pacote nenhum**; a discrição de forma dos gráficos é explicitamente «sem biblioteca externa» |
</threat_model>

<verification>
1. `npm run build` compila e exporta; o total de páginas continua 1.931 — a rota já existe.
2. `npm run verificar-fase2`, `verificar-comentado` e `verificar-fase4` continuam verdes.
3. `verificar-fase3` vermelha só no gate 8 de `globals.css`, herdado de 05-01.
4. Console sem erro nem aviso da aplicação; **zero requisição externa**, medida por dentro da
   página — é o gate que uma biblioteca de gráfico derrubaria.
5. `git diff --stat` não mostra `globals.css`, `web.css`, `mapa.css` nem `desertos.tsx`.
</verification>

<success_criteria>
- Painel de procedência na primeira vista, com seis fatias somando 7.810 e 66.563, barras
  proporcionais medidas, e a frase sobre `autorado` dita sem suavização.
- Cinco ou mais indicadores, cada um com denominador e procedência do número visíveis.
- Gratuidade declarada como não sustentada, com os dois denominadores.
- Quatro públicos trocando o recorte sem trocar de tela nem de URL.
- Mapa de desertos na tela, com Sergipe e Tocantins nomeados e a distinção intacta.
- Tudo sobrevive ao modo comentado desligado; nada transborda a janela.
- Peso de chunks cresce no máximo 60 KB.
- A sonda descartável foi apagada.
</success_criteria>

<output>
Create `.planning/phases/05-camada-2-web-desktop-e-profundidade/05-05-SUMMARY.md` when done
</output>
