---
phase: 05-camada-2-web-desktop-e-profundidade
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/dados/redacao.ts
  - src/componentes/redacao-fila.tsx
  - src/componentes/redacao-trilha.tsx
  - src/app/(bastidor)/redacao/fila/page.tsx
  - src/app/(bastidor)/redacao/trilha/page.tsx
  - src/estilos/redacao.css
autonomous: true
requirements: [WEB-05, WEB-06]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A fila mostra a origem de cada item — produtor, ingestão automática ou sugestão de IA — e os de IA trazem score de confiança visível (D-82)"
    - "Vetar sem motivo escrito NÃO conclui: o botão de confirmar veto fica desabilitado enquanto o campo estiver vazio, e nenhuma decisão de veto é registrada (D-83)"
    - "Toda decisão registra quem decidiu e quando, e o escopo do curador aparece no topo da tela (D-84)"
    - "No editor de trilha, um passo sem motivo impede a publicação — e a tela diz qual passo e por quê (D-85)"
    - "O motivo que o editor mostra para os passos da trilha existente é IDÊNTICO, caractere a caractere, ao selo que /trilha/[slug]/ mostra ao público (D-85, as duas pontas concordando)"
    - "A sugestão da IA é sempre descartável e o rodapé declara os três limites: não publica, não define destaque, não escreve verbete (D-86)"
  artifacts:
    - src/dados/redacao.ts
    - src/componentes/redacao-fila.tsx
    - src/componentes/redacao-trilha.tsx
    - src/estilos/redacao.css
  key_links:
    - "redacao.ts → grafo.ts e trilha.ts (a fila e os passos saem do acervo, não de literal escrito à mão)"
    - "redacao-trilha.tsx → passosDaTrilha/trilhaEhPublicavel de trilha.ts — a MESMA fonte que /trilha/[slug]/ lê, e é por construção que os textos batem"
    - "o veto: campo de motivo → estado desabilitado do botão → ausência de registro de decisão"
---

<objective>
As duas telas de Redação passam a existir: a fila de moderação em que a IA sugere e o humano
assina, e o editor de trilha em que o motivo por passo é campo obrigatório.

Purpose: é aqui que o protótipo responde à pergunta mais difícil do RFP — qual é o limite da
IA. A resposta não é uma frase no rodapé: é uma tela em que a sugestão da IA chega marcada,
com score, e não vira dado público sem um humano pôr o nome; e um botão de veto que não
conclui sem motivo escrito. A diferença entre curadoria e moderação silenciosa é essa
obrigatoriedade, e ela precisa ser demonstrável ao vivo em vez de descrita.

Output: `/redacao/fila/` e `/redacao/trilha/` preenchidas, nas rotas que já existem.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/5-CONTEXT.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/05-01-SUMMARY.md
@.planning/phases/04-camada-1-studio-e-o-roteiro/04-02-SUMMARY.md
@.planning/phases/04-camada-1-studio-e-o-roteiro/04-03-SUMMARY.md
@src/componentes/studio-duplicatas.tsx
@src/dados/trilha.ts
@src/dados/ocorrencias-studio.ts
@src/estilos/studio.css
@src/estilos/web.css
</context>

<contrato_herdado>
**Leia `05-01-SUMMARY.md` antes de escrever uma linha.**

1. **`src/estilos/redacao.css` já existe, vazia de regra.** É sua, e é a ÚNICA folha que este
   plano escreve. **Não toque em `src/app/globals.css`** nem em `src/estilos/web.css` nem em
   `src/estilos/studio.css` — a última é da fase 4 e há gates herdados sobre ela.
2. Deste plano são os atributos `data-fila-redacao`, `data-item-fila`, `data-procedencia-item`,
   `data-score-ia`, `data-acao-redacao`, `data-motivo-veto`, `data-veto-bloqueado`,
   `data-decisao-redacao`, `data-escopo-curador`, `data-passo-trilha`, `data-motivo-passo`,
   `data-publicavel`, `data-sugestao-ia` e `data-limites-ia`. Mais `data-nao-sustenta`, que a
   fase 4 fixou e a fase 5 inteira reaproveita para as declarações de D-90.
3. **Orçamento de chunks deste plano: 60 KB.**

`npm run verificar-fase3` está vermelha no gate 8 (`globals.css` ancorado em `c03f627`) desde
o commit de 05-01. Esperado; reancoramento é de 05-08; **não conserte aqui**. Para regressão
use `verificar-fase2` e `verificar-fase4`.

**Estas duas rotas são de bastidor.** O layout de `(bastidor)` já monta `<AvisoDesktop>` e
esconde o conteúdo na visão app — Redação existe só na web, que é a única exceção que D-05
autoriza. Você não precisa fazer nada para isso acontecer, e **não pode desfazê-lo**. O
bastidor **não monta barra de abas**: a medida de «cabe na primeira vista» é a moldura inteira,
sem descontar os 59 px da barra.
</contrato_herdado>

<protocolo_de_disco>
Volume a 96% com histórico de despejo do iCloud — `stat` informa tamanho, a leitura devolve
zero byte, e uma folha de estilo já foi destruída em definitivo por ter sido commitada depois
do despejo. Confirme cada arquivo existente com `wc -c` contra `git show HEAD:<arquivo> | wc -c`
**antes de editar**; divergiu ou leu zero, restaure com `git show HEAD:<arquivo> > <arquivo>`,
nunca com `brctl download`. Antes de cada commit leia `git diff --cached --numstat`; depois
confira os bytes NO GIT e leia `git diff-tree --name-status -r HEAD`. Empurre com
`git push espelho HEAD`. Confira o STATE.md depois de qualquer `state.update-progress`.
</protocolo_de_disco>

<tasks>

<task type="auto">
  <name>Task 1: redacao.ts — a fila das três origens e os passos da trilha, montados no build a partir do acervo</name>
  <files>src/dados/redacao.ts</files>
  <action>
    Crie `src/dados/redacao.ts`, módulo de build puro, síncrono e memoizado, no molde de
    `duplicatas.ts` e `ocorrencias-studio.ts`. Importa `grafo.ts` e `trilha.ts` **por valor** e
    roda só no build; nenhum arquivo `"use client"` pode importá-lo por valor, só
    `import type` (DP-F).

    **A fila (D-82).** Ela é encenada — a tela 34 diz «fila encenada com itens das três
    origens» — mas **os itens são entidades reais do acervo**, não texto inventado: cada item
    da fila aponta para uma entidade existente e mostra os campos dela. O que é autorado é a
    ATRIBUIÇÃO de origem e o score, e a tela rotula isso como autorado, com `procedencia:
    "autorado"` visível, do mesmo jeito que a fase 4 rotulou as 40 duplicatas encenadas.

    Três origens, no tipo `OrigemDoItem`: `"produtor"`, `"ingestao"`, `"ia"`. Só os de `"ia"`
    trazem `score: number` — os outros trazem `score: null`, porque **produtor e ingestão não
    estimam, eles afirmam**, e um score nos três achataria a distinção que a tela existe para
    fazer. O score é gerado por regra determinística e declarada sobre o próprio dado (por
    exemplo: completude da ficha, presença de imagem, presença de linguagem), **nunca por
    número aleatório nem por literal escolhido a dedo** — exporte a regra em texto para a tela
    imprimir, como `duplicatas.ts` faz com `CRITERIO_DE_IDENTIDADE`. Um score sem regra
    auditável é exatamente o recomendador opaco contra o qual a proposta inteira se posiciona.

    Escolha os itens por regra determinística e ordenada por chave estável, nunca por
    `localeCompare` e nunca por índice de array. Dimensione a fila para caber na tela e ser
    percorrível ao vivo — a ordem de grandeza da fila de duplicatas da fase 4, 84 itens, é boa
    referência de teto; abaixo dela também serve, desde que as três origens estejam bem
    representadas e o número seja declarado.

    Exporte `filaDaRedacao(): ItemDaFila[]`, `itemDaFilaPorId(id)`,
    `REGRA_DO_SCORE: string`, `ESCOPOS_DE_CURADORIA: readonly Escopo[]` (nacional, territorial
    e por linguagem — D-84, e são eles que respondem ao «como crescer sem reescrever» do RFP,
    então cada um traz o número de itens que ele alcança, medido), `LIMITES_DA_IA: readonly
    string[]` com os três limites de D-86 escritos por extenso, e
    `numerosDaRedacao(): NumerosDaRedacao` com tudo que a tela e o gate de 05-08 vão citar.

    **Os passos da trilha (D-85).** Exporte `passosParaEditor(slug)` construído SOBRE
    `passosDaTrilha` e `trilhaCompletaPorSlug` de `trilha.ts` — **não reescreva a travessia**.
    Isto não é economia de código: é o que faz o motivo que o editor mostra ser o MESMO objeto
    que `/trilha/[slug]/` mostra ao público, em vez de uma segunda cópia que diverge na
    primeira edição. D-85 exige que as duas pontas concordem, e a única forma de garantir isso
    é elas lerem a mesma fonte.

    Exporte `catalogoParaArrastar()` — as entidades candidatas a virar passo, de qualquer
    classe (tela 35: «o grafo completo como fonte de arrasto»). **Meça o peso**: o grafo tem
    7.810 entidades e mandar todas para o navegador estoura o orçamento. Recorte por regra
    declarada, exporte o recorte e o total, e ponha os dois na tela: «1.200 de 7.810
    candidatos, recortados por X» é honesto, «o grafo completo» quando são 1.200 não é.

    **Teto deste módulo: 60 KB de DTO somando fila e catálogo.** Meça e exporte o número.

    Faça o módulo **quebrar alto** se a trilha do traçador deixar de existir ou se um passo
    vier com motivo vazio: `PassoTrilha.motivo` é documentado como nunca vazio, e um motivo
    vazio chegando aqui significaria que a fonte mudou por baixo — melhor parar de compilar
    que publicar um selo em branco.
  </action>
  <verify>
    <automated>node_modules/.bin/tsx -e "
import {filaDaRedacao, REGRA_DO_SCORE, ESCOPOS_DE_CURADORIA, LIMITES_DA_IA, numerosDaRedacao, passosParaEditor, catalogoParaArrastar} from '@/dados/redacao';
import {trilhas} from '@/dados/trilha';
const fila = filaDaRedacao();
const origens = fila.reduce((a:any,i:any)=>(a[i.origem]=(a[i.origem]||0)+1,a),{});
if(!origens.produtor||!origens.ingestao||!origens.ia) throw new Error('as tres origens: '+JSON.stringify(origens));
const comScore = fila.filter((i:any)=>i.score!==null);
if(comScore.length!==origens.ia) throw new Error('score em '+comScore.length+' itens, ia sao '+origens.ia);
if(comScore.some((i:any)=>i.score<0||i.score>1)) throw new Error('score fora de 0..1');
if(REGRA_DO_SCORE.length<80) throw new Error('REGRA_DO_SCORE curta demais para ser auditavel');
if(LIMITES_DA_IA.length!==3) throw new Error('LIMITES_DA_IA: '+LIMITES_DA_IA.length);
if(ESCOPOS_DE_CURADORIA.length!==3) throw new Error('ESCOPOS: '+ESCOPOS_DE_CURADORIA.length);
const slug = trilhas()[0].slug;
const passos = passosParaEditor(slug);
if(!passos.length) throw new Error('sem passos');
if(passos.some((p:any)=>!p.motivo||!p.motivo.trim())) throw new Error('passo com motivo vazio');
const cat = catalogoParaArrastar();
const bytes = JSON.stringify({fila, cat}).length;
if(bytes > 60*1024) throw new Error('DTO '+bytes+' bytes, teto 61440');
console.log('OK', JSON.stringify(origens), 'passos='+passos.length, 'catalogo='+cat.itens.length+'/'+cat.total, bytes+' bytes');
console.log(JSON.stringify(numerosDaRedacao()));
"</automated>
  </verify>
  <done>O módulo devolve fila com as três origens, score só nos de IA e dentro de 0..1, uma regra de score auditável, os três limites da IA, os três escopos com alcance medido, os passos da trilha lidos de `trilha.ts` com motivo não-vazio, e um DTO abaixo de 60 KB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /redacao/fila — a fila com origem e score, e o veto que não conclui sem motivo</name>
  <files>src/componentes/redacao-fila.tsx, src/app/(bastidor)/redacao/fila/page.tsx, src/estilos/redacao.css</files>
  <behavior>
    - Ao carregar a tela do zero, `data-decisao-redacao` mede ZERO: nada foi decidido sem um
      humano clicar, e as quatro ações estão visíveis esperando.
    - Todo item da fila tem `data-procedencia-item` com um de três valores; a contagem de
      itens com `data-score-ia` é igual à contagem de itens com origem `ia`, e zero fora dela.
    - Clicar «vetar» abre o campo `data-motivo-veto`; com o campo VAZIO, o botão de confirmar
      tem `data-veto-bloqueado="sim"` e a propriedade `disabled` verdadeira, e clicar nele NÃO
      cria decisão nenhuma.
    - Digitado um motivo, `data-veto-bloqueado` passa a `"nao"`, o botão habilita, e o clique
      cria exatamente UMA decisão, carregando o texto do motivo, quem decidiu e quando.
    - Aprovar, editar e devolver também registram decisão com autor e carimbo.
    - O escopo do curador está visível no topo com `data-escopo-curador`, trocá-lo troca o
      recorte da fila e NÃO troca de URL.
    - O rodapé `data-limites-ia` está visível com o modo comentado DESLIGADO e traz os três
      limites.
  </behavior>
  <action>
    `page.tsx` é componente de servidor e é ele que toca `redacao.ts`; o componente de cliente
    recebe o DTO por propriedade e não alcança `@/dados/grafo` nem transitivamente (DP-F).

    **O veto é o coração da tela (D-83) e a sua correção é estrutural, não de aparência.** O
    botão de confirmar veto tem de estar de fato `disabled` — não só parecer apagado — e a
    função que registra a decisão tem de recusar motivo em branco por conta própria, mesmo que
    alguém chame por outro caminho. Duas travas, porque a de aparência sozinha vira decisão
    registrada quando o teclado ativa o botão. `data-veto-bloqueado` reflete o estado real do
    botão, não um espelho separado que pode divergir.

    Espaço em branco não é motivo: apare antes de avaliar. E o motivo é **guardado com a
    decisão e mostrado no registro**, porque um veto cujo motivo some é moderação silenciosa
    com um passo a mais.

    As quatro ações (D-83): aprovar, editar, vetar, devolver, cada uma com
    `data-acao-redacao` e o seu valor. Só o veto exige motivo; devolver pode pedir um
    comentário opcional, e se pedir, deixe claro na tela que ali é opcional e no veto não é —
    a assimetria é o conteúdo.

    **Autoria e horário (D-84).** Toda decisão carrega quem decidiu e quando, no molde do
    registro de decisão da fase 4. **O carimbo vem da data de referência do build**, jamais do
    relógio do runtime: sob export estático um relógio de runtime faz o HTML exportado e a
    hidratação divergirem e ainda vaza o fuso de quem avalia. Reaproveite o padrão de
    `DATA_DE_REFERENCIA_DO_STUDIO` e `CARIMBO_DA_ALTERACAO` de `ocorrencias-studio.ts` — leia
    como ele foi feito antes de escrever o seu.

    **O escopo do curador (D-84, D-89).** Nacional, territorial ou por linguagem, no topo, com
    o número de itens que cada escopo alcança. Trocar o escopo troca o RECORTE, não a tela e
    não a URL — é a mesma superfície servindo cortes diferentes, e é isso que responde ao «como
    crescer sem reescrever» do RFP. Ponha o argumento em `<Comentario>`; o controle e o número
    ficam fora dele.

    **Os limites da IA (D-86)** vão no rodapé com `data-limites-ia`, **fora de `<Comentario>`**:
    é produto, não nota de protótipo, e tem de continuar na tela com o modo comentado
    desligado. Os três: não publica, não define destaque, não escreve verbete.

    Junto do score, a **regra que o produziu**, visível — `REGRA_DO_SCORE` por extenso, como a
    fase 4 pôs o critério de identidade na tela. Score sem regra é o oposto do que esta tela
    demonstra.

    Onde o acervo não sustentar um campo que a tela 34 pede, declare com `data-nao-sustenta` e
    o denominador medido (D-90). Não deixe campo vazio sem frase.

    Escreva as regras em `src/estilos/redacao.css` e só nela. Reaproveite o vocabulário visual
    de `src/estilos/studio.css` **por classe, sem editá-la** — as telas de bastidor têm de
    parecer a mesma família, e `studio.css` pertence à fase 4.

    **Geometria.** A fila é comprida e a fase 4 pagou duas vezes por gate verde sobre tela
    quebrada: garanta que nenhuma linha e nenhum botão de ação saia da janela, e que o painel
    do item escolhido não seja coberto. O gate mede retângulos.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-04.ts fila</automated>
  </verify>
  <done>No DOM vivo a 1440×960 na visão web: zero decisão antes do clique, score só nos itens de IA, veto bloqueado com campo vazio e sem decisão criada, veto concluído com motivo criando exatamente uma decisão com autor e carimbo, escopo trocando o recorte sem trocar de URL, limites da IA visíveis com o modo comentado desligado, e nada fora da janela.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /redacao/trilha — o motivo por passo como campo obrigatório, e a sugestão da IA sempre descartável</name>
  <files>src/componentes/redacao-trilha.tsx, src/app/(bastidor)/redacao/trilha/page.tsx</files>
  <behavior>
    - O editor abre com os passos da trilha existente, cada um com `data-passo-trilha` e um
      `data-motivo-passo` preenchido.
    - O texto de `data-motivo-passo` de cada passo é IDÊNTICO, caractere a caractere, ao motivo
      que `/trilha/[slug]/` mostra ao público para o mesmo passo.
    - Acrescentar um passo do catálogo cria um passo com motivo VAZIO; nesse estado
      `data-publicavel` vale `"nao"` e a tela nomeia qual passo está sem motivo.
    - Preenchido o motivo daquele passo, `data-publicavel` volta a `"sim"`.
    - O botão de publicar está `disabled` sempre que `data-publicavel` for `"nao"`.
    - A sugestão da IA tem `data-sugestao-ia`, um controle de descartar, e descartá-la remove-a
      da tela sem alterar a trilha.
    - O rodapé `data-limites-ia` está visível com o modo comentado desligado.
  </behavior>
  <action>
    `page.tsx` é de servidor e monta o DTO por `passosParaEditor` e `catalogoParaArrastar` de
    `redacao.ts`. O componente de cliente guarda o rascunho em `localStorage`, sob uma chave
    nova do espaço `agenda-cultural:` — escolha o nome, declare-o no SUMMARY, e **não toque em
    `src/contexto/sessao.tsx`**, que é compartilhado e pertence a outra fase.

    **D-85 é a regra dura: passo sem motivo não publica.** Como no veto, duas travas: o botão
    de publicar de fato `disabled`, e a função de publicar recusando por conta própria. E a
    tela **nomeia qual passo** está sem motivo — «não é publicável» sem dizer onde é um beco, e
    esta é a fase que resolveu não ter becos. Reaproveite `trilhaEhPublicavel` de `trilha.ts`
    para os três casos que ela já cobre e some o quarto, que é o motivo em branco; não
    reimplemente os três.

    **As duas pontas têm de concordar.** O motivo que este editor mostra é o mesmo campo
    `motivo` de `PassoTrilha`, lido de `trilha.ts` via `redacao.ts` — não copie o texto, não
    reformate, não acrescente prefixo. É esse motivo que vira o selo visível ao público, e
    05-08 vai comparar os dois textos caractere a caractere. `origemMotivo` também vai para a
    tela: um motivo escrito no acervo e um composto a partir da relação não são a mesma coisa,
    e a distinção já existe no tipo.

    **Arrastar (tela 35).** Montagem por arrasto de entidades de qualquer classe. Se o arrasto
    nativo do navegador se mostrar frágil de dirigir por CDP, **ofereça também um caminho por
    clique** — um botão «acrescentar» em cada candidato — e faça o gate dirigir esse caminho.
    Não troque o arrasto pelo clique: ofereça os dois. Uma interação que o gate não consegue
    dirigir é uma interação que ninguém prova, e o RFP vai ver a tela ao vivo.

    **A sugestão da IA (D-86).** Ela sugere o PRÓXIMO passo, com a regra determinística que a
    produziu declarada ao lado — travessia do grafo a partir do último nó, não modelo. Ela é
    **sempre descartável**: um controle de descartar visível, e descartar não altera nada da
    trilha. Nenhuma sugestão entra na trilha sem um clique humano. O rodapé com os três limites
    fica **fora de `<Comentario>`**.

    Prévia na visão app (tela 35): mostre como a trilha aparecerá ao público. **Não crie um
    componente irmão** para isso — se precisar da aparência de app dentro da visão web, use um
    contêiner com regra própria em `redacao.css` que reproduza a moldura, e diga na tela que é
    prévia. Se isso ficar caro, prefira um link para `/trilha/[slug]/` a uma prévia falsa.

    Assinatura do curador e agendamento de publicação (tela 35) entram como campos reais com
    autoria e carimbo, pelo mesmo padrão de data de referência do build da Task 2.

    Escreva regras só em `src/estilos/redacao.css`, que a Task 2 já começou — este plano é o
    dono dela e não há concorrência dentro do próprio plano.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-04.ts trilha</automated>
  </verify>
  <done>No DOM vivo: o editor abre com os passos e motivos da trilha existente, o texto de cada motivo bate caractere a caractere com o que `/trilha/[slug]/` mostra, acrescentar um passo sem motivo põe `data-publicavel="nao"` com o passo nomeado e o botão de publicar desabilitado, preencher o motivo devolve a `"sim"`, e a sugestão da IA some ao ser descartada sem alterar a trilha.</done>
</task>

</tasks>

<sonda_do_plano>
As tasks 2 e 3 pedem `scripts/sonda-05-04.ts`, sonda descartável — **apague antes do último
commit** e confirme `git status --short scripts/` vazio. A suíte permanente é de 05-08.

API real de `scripts/navegador.mjs`, que é esta e não a presumida: `servir({raiz})` de
`servir-out.mjs` devolve `{url, fechar}`; `abrirNavegador()` devolve o `cdp` diretamente; o
método é `cdp.navegar(url)`; console em `cdp.consola`; **`cdp.clicar` recebe uma EXPRESSÃO
JavaScript que avalia para o elemento**, não um seletor CSS; `cdp.avaliar(expressao)` e
`naPagina(corpo)` embrulham com o preludio. **Não existe `Network.*`** — prove rede por
`performance.getEntriesByType('resource')` de dentro da página. `visiveis()` falha sobre SVG.

Visão web por `localStorage.setItem("agenda-cultural:visao","web")` (string crua, não JSON)
mais recarregar; espere `data-hidratado="sim"`.

**Atributo que só existe durante uma interação mede 0 no HTML exportado.**
`data-motivo-veto`, `data-veto-bloqueado`, `data-decisao-redacao` e `data-sugestao-ia`
descartada são desse tipo: meça-os no DOM vivo **depois de dirigir o gesto**, e no HTML
exportado conte-os esperando zero, documentando que o zero é de propósito. É exatamente a
armadilha que 04-03 nomeou e 04-05 transformou em gate.

Para digitar num campo controlado do React, definir `.value` direto não dispara o `onChange` —
use o setter nativo do protótipo de `HTMLTextAreaElement`/`HTMLInputElement` e despache um
evento `input` com `bubbles: true`, depois espere um `requestAnimationFrame`.

**Onde o gate afirmar usabilidade, meça geometria contra o contêiner**: `scrollWidth` contra
`clientWidth`, retângulo de cada botão de ação contra o da janela, retângulo do painel do item
escolhido. Presença e visibilidade já deram verde sobre tela quebrada duas vezes nesta obra.
Defina `DIR_CAPTURAS` e guarde fotos das duas telas **fora do repositório**, para julgamento
humano — em especial a do estado de veto bloqueado, que é o que a banca vai querer ver.

Para a comparação de D-85, leia o `innerText` do selo de motivo em `/trilha/[slug]/` e o
`value` do campo de motivo no editor e compare com igualdade estrita depois de aparar as
pontas. Compare data por número, nunca por string: o projeto exibe `DD.MM.AAAA` e guarda ISO.
</sonda_do_plano>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curador → dado público | a fila é onde algo vira dado público; o veto é o controle que impede isso |
| IA → dado público | a sugestão da IA nunca atravessa sem clique humano — é o limite que a tela demonstra |
| localStorage → tela | o rascunho da trilha é editável pelo avaliador e não é confiável |
| build → cliente | `redacao.ts` alcança o grafo de 23 MB e não pode ser importado por valor do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Elevation of privilege | veto concluindo sem motivo pelo teclado ou por outro caminho de chamada | critical | mitigate | duas travas — botão de fato `disabled` E a função de registro recusando motivo em branco aparado; gate dirige o clique com campo vazio e exige zero decisão |
| T-05-15 | Elevation of privilege | sugestão da IA entrando na trilha sem clique humano | critical | mitigate | sugestão é objeto separado da trilha até o clique; gate descarta a sugestão e exige a trilha inalterada |
| T-05-16 | Repudiation | decisão sem autor ou sem carimbo | high | mitigate | autor e carimbo obrigatórios no registro; carimbo vem da data de referência do build, nunca do relógio de runtime |
| T-05-17 | Tampering | rascunho adulterado em localStorage quebrando a tela | medium | mitigate | valor que não é lista devolve rascunho vazio, no molde de `lerLista` de `sessao.tsx`; gate exercita valor inválido |
| T-05-18 | Spoofing | motivo do editor divergir do selo público | high | mitigate | editor e tela pública leem o MESMO campo de `trilha.ts`; gate compara os textos caractere a caractere |
| T-05-19 | Information disclosure | catálogo de arrasto mandando as 7.810 entidades ao navegador | high | mitigate | recorte declarado com total, teto de 60 KB medido no gate da Task 1 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **este plano não instala pacote nenhum**; zero dependência nova |
</threat_model>

<verification>
1. `npm run build` compila e exporta; o total de páginas continua 1.931 — as duas rotas já
   existem e este plano não acrescenta nenhuma.
2. `npm run verificar-fase2`, `verificar-comentado` e `verificar-fase4` continuam verdes.
3. `verificar-fase3` vermelha só no gate 8 de `globals.css`, herdado de 05-01.
4. Console sem erro nem aviso da aplicação; zero requisição externa.
5. `git diff --stat` não mostra `globals.css`, `web.css`, `studio.css` nem `sessao.tsx`.
</verification>

<success_criteria>
- Fila com as três origens marcadas, score só nos de IA, e a regra do score visível.
- Veto sem motivo não conclui e não registra — provado dirigindo o clique com o campo vazio.
- Toda decisão traz autor e carimbo vindos da data de referência do build.
- Escopo do curador no topo, trocando o recorte sem trocar de URL, com alcance medido.
- Passo sem motivo bloqueia a publicação e a tela nomeia o passo.
- O motivo do editor bate caractere a caractere com o selo público da trilha.
- Sugestão da IA descartável, e os três limites visíveis com o modo comentado desligado.
- Peso de chunks cresce no máximo 60 KB.
- A sonda descartável foi apagada.
</success_criteria>

<output>
Create `.planning/phases/05-camada-2-web-desktop-e-profundidade/05-04-SUMMARY.md` when done
</output>
