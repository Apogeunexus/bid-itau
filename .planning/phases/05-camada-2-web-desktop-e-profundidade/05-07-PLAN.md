---
phase: 05-camada-2-web-desktop-e-profundidade
plan: 07
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/dados/play.ts
  - src/componentes/play.tsx
  - src/componentes/player.tsx
  - src/app/(app)/play/page.tsx
  - src/app/(app)/play/[slug]/page.tsx
  - src/estilos/play.css
autonomous: true
requirements: [APPX-02, APPX-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Play é catálogo unificado das 529 mídias reais, recortável pelas categorias que o próprio acervo declara (D-92)"
    - "O bloco «não pode ir? veja isto» liga mídia a evento pelas arestas reais, e declara com o denominador quantas das 529 têm essa ligação (D-92, D-90)"
    - "O filtro de acessibilidade do Play declara, com denominador, quantas das 529 têm Libras, legenda e audiodescrição — em vez de oferecer três filtros equivalentes"
    - "O Player registra no repertório ao concluir, e o registro sobrevive a recarregar (D-92)"
    - "O Player declara que o protótipo não carrega arquivo de mídia nenhum, e por quê — nenhuma requisição sai para fora do servidor local"
  artifacts:
    - src/dados/play.ts
    - src/componentes/play.tsx
    - src/componentes/player.tsx
    - src/estilos/play.css
  key_links:
    - "play.ts → vizinhos(fala_sobre) de grafo.ts — a única ligação real entre mídia e evento no acervo"
    - "player.tsx → uma chave nova em agenda-cultural: no localStorage, lida de volta por play.tsx em «continue de onde parou»"
    - "play/[slug]/page.tsx → generateStaticParams sobre slugsPorTipo('midia')"
---

<objective>
Play deixa de ser esqueleto e passa a ser o catálogo unificado das 529 mídias reais do acervo,
e o Player existe com acessibilidade em evidência e registro no repertório ao concluir.

Purpose: Play sustenta dois argumentos do RFP ao mesmo tempo — o da gratuidade e o da escala
nacional onde não há equipamento cultural. Ele é a resposta para as regiões que o mapa de
desertos mostra vazias: quando não há teatro na cidade, o que existe é isto. E o bloco «não
pode ir? veja isto» é a ponte entre o que está em cartaz e o que se pode ver de casa.

Output: `/play/` preenchida e `/play/[slug]/` criada, uma rota por mídia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/5-CONTEXT.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/05-01-SUMMARY.md
@src/app/(app)/play/page.tsx
@src/componentes/ficha-acessibilidade.tsx
@src/componentes/capa-sem-imagem.tsx
@src/contexto/sessao.tsx
@src/dados/repertorio.ts
@src/estilos/web.css
</context>

<contrato_herdado>
**Leia `05-01-SUMMARY.md` antes de escrever uma linha.**

1. **`src/estilos/play.css` já existe, vazia de regra.** É sua, e é a única folha deste plano.
   **Não toque em `src/app/globals.css`** nem em `src/estilos/web.css`.
2. Deste plano são `data-play`, `data-categoria`, `data-midia`, `data-player`,
   `data-concluir`, `data-assistido`, `data-veja-isto` e `data-sem-arquivo`. Mais
   `data-nao-sustenta`, que a fase 5 inteira reaproveita para D-90.
3. **Orçamento de chunks deste plano: 100 KB** — o maior da onda, porque o catálogo de 529
   mídias é o maior DTO da fase. Não o estoure: o teto de `out/_next/static/chunks` é 1.600 KB
   e há gate herdado que reprova.

`npm run verificar-fase3` está vermelha no gate 8 (`globals.css` ancorado em `c03f627`) desde
o commit de 05-01. Esperado; o reancoramento é de 05-08; **não conserte aqui**. Para regressão
use `verificar-fase2` e `verificar-fase4`.

**Este plano acrescenta 529 rotas ao artefato** — uma por mídia, em `/play/[slug]/`. É o maior
acréscimo da fase. **Meça e registre no SUMMARY o total de páginas depois do seu build, e o
número de rotas em `out/play/`**, porque 05-08 precisa dos dois para acrescentar o padrão à
lista de rotas explicáveis do gate da fase 3 **sem mover o limiar de 1.784**. Um outro plano
desta onda também acrescenta rota; **não afirme um total absoluto no seu gate** — afirme o que
é seu.

**`src/contexto/sessao.tsx` é compartilhado e você não pode editá-lo.** A chave de conclusão
do Player é sua e vive fora dele.
</contrato_herdado>

<protocolo_de_disco>
Volume a 96% com histórico de despejo do iCloud — `stat` informa tamanho, a leitura devolve
zero byte. Confirme cada arquivo existente com `wc -c` contra `git show HEAD:<arquivo> | wc -c`
**antes de editar**; divergiu ou leu zero, restaure com `git show HEAD:<arquivo> > <arquivo>`,
nunca com `brctl download`. Antes de cada commit leia `git diff --cached --numstat`; depois
confira os bytes NO GIT e leia `git diff-tree --name-status -r HEAD`. Empurre com
`git push espelho HEAD`. Confira o STATE.md depois de qualquer `state.update-progress`.
</protocolo_de_disco>

<o_que_foi_medido_antes_de_planejar>
Apurado contra o grafo em disco durante o planejamento. **Confirme cada número calculando-o —
não copie estes literais para dentro do código.**

**As 529 mídias, por `extra.categoria` — e são elas o «catálogo unificado» da tela 19:**
podcasts **336** · series **63** · videos **46** · noticias **45** · entrevista **25** ·
colunistas **7** · playlists **4** · agenda-cultural **2** · acervos **1**. Somam 529.
**Não existe campo de tipo nem de formato**; `categoria` é o que o acervo tem, e é dela que o
recorte sai. Vídeo, podcast, série e playlist da tela 19 estão todos ali, com nomes do CMS.

**518 das 529 têm imagem local.** Todas as 529 têm `resumo`, `fonte` (uma URL de
itaucultural.org.br) e `extra.publicadoEm`, entre 2016-07 e 2026-01. **352** têm linguagem,
**242** têm tema.

**Nenhuma mídia carrega arquivo de áudio ou vídeo.** O acervo tem a ficha e a capa, não o
arquivo. `fonte` aponta para o site do Itaú Cultural — e **buscar de lá quebraria o gate de
zero requisição externa**, que é uma promessa medida da fase 2 em diante e vale 125 navegações.

**Acessibilidade das mídias:** as 529 declaram a ficha, e apenas **3** têm `libras`
verdadeiro. `audio_description`, `closed_caption`, `subtitle` e as demais medem **zero** nas
529. A tela 19 pede filtro por legenda, Libras e audiodescrição: **um deles recorta 3 de 529 e
os outros dois recortam zero**, e é isso que a tela tem de dizer (D-90).

**A ligação com evento.** A única relação real entre mídia e evento no acervo é `fala_sobre`:
**34 arestas**, saindo de **14 mídias distintas**. `semelhante_a` de mídia vai só para outra
mídia (5.147 arestas) e nunca para evento. Portanto **«não pode ir? veja isto» é sustentado por
14 das 529**, e o bloco declara isso com o denominador em vez de fingir cobertura. **Não
autore aresta mídia→evento para inflar o número**: seria afirmar que aquele vídeo fala daquele
evento quando ninguém disse isso.

**A leitura de projeto que sai daí, e ela é forte:** a ponte que existe de verdade é a
inversa. As 34 arestas partem da mídia e chegam ao evento, então a página do evento pode
oferecer «não pode ir? veja isto» com lastro para os eventos alcançados, e o Play declara a
cobertura real. Meça quantos eventos distintos as 34 arestas alcançam e ponha o número na tela.
</o_que_foi_medido_antes_de_planejar>

<tasks>

<task type="auto">
  <name>Task 1: play.ts — o catálogo das 529, as categorias reais, e a cobertura medida da ponte com evento</name>
  <files>src/dados/play.ts</files>
  <action>
    Crie `src/dados/play.ts`, módulo de build puro, síncrono e memoizado, no molde de
    `duplicatas.ts`. Importa `grafo.ts` **por valor**, roda só no build, e nada `"use client"`
    o importa por valor (DP-F).

    **`catalogoDoPlay(): ItemDoPlay[]`** — as 529, cada uma com `id`, `slug`, `titulo`,
    `rota`, `categoria`, `rotuloCategoria`, `resumo`, `imagem`, `creditoImagem`,
    `publicadoEm`, `linguagens`, `procedencia`, `fonte`, `acessibilidade` (as 8 dimensões) e
    `declaraAcessibilidade`. **Rótulo de categoria em português vindo de tabela declarada
    aqui**, não do valor cru do CMS: `podcasts` vira «Podcast» na tela, e a tabela é o único
    lugar onde essa tradução mora.

    **`CATEGORIAS: readonly CategoriaContada[]`** — uma por categoria presente, com `valor`,
    `rotulo` e `n` **contado**, ordenada por `n` decrescente e depois por chave estável. Nunca
    lista escrita à mão: a categoria sai do dado, como as facetas de `indice.ts` saem do
    índice.

    **`ACESSIBILIDADE_DAS_MIDIAS`** — as 8 dimensões contadas sobre as 529, iterando pela
    chave do tipo `Acessibilidade` e não por lista paralela, cada uma com `n`, `de` (529) e
    `sustentada`. É o que sustenta o filtro honesto da tela 19.

    **`vejaIsto(eventoId)` e `PONTE_COM_EVENTO`.** A ligação sai de `vizinhos(midiaId,
    "fala_sobre")` filtrado por classe `evento`; a leitura útil na página do evento é a
    inversa. `PONTE_COM_EVENTO` traz `arestas`, `midiasDistintas`, `eventosAlcancados` e
    `deQuantas` (529) — todos contados — e `declaracao`, a frase de D-90 com o denominador.

    **`continuarDeOndeParou`** não mora aqui: é estado de navegador. O que mora aqui é a
    resolução `slug → item`, por `itemDoPlay(slug)`, para o Play resolver o que o
    `localStorage` guardou sem precisar varrer o catálogo no cliente.

    **O teto é o ponto duro deste plano.** 529 itens com resumo integral estouram 100 KB
    facilmente. Meça o DTO serializado e, se passar de **100 KB**, **corte campo, nunca item**:
    trunque o resumo no catálogo e mande o resumo inteiro só na rota do player, que é de
    servidor e não paga chunk. Exporte os bytes medidos e o que foi cortado, e ponha os dois na
    tela — o custo declarado é o padrão que o feed e o índice de busca já fixaram.

    Faça o módulo **quebrar alto** se o catálogo não tiver 529 itens ou se as categorias não
    somarem 529.
  </action>
  <verify>
    <automated>node_modules/.bin/tsx -e "
import {catalogoDoPlay, CATEGORIAS, ACESSIBILIDADE_DAS_MIDIAS, PONTE_COM_EVENTO, itemDoPlay} from '@/dados/play';
const c = catalogoDoPlay();
if(c.length!==529) throw new Error('catalogo: '+c.length);
const soma = CATEGORIAS.reduce((a:number,x:any)=>a+x.n,0);
if(soma!==529) throw new Error('categorias somam '+soma);
const pod = CATEGORIAS.find((x:any)=>x.valor==='podcasts');
if(!pod||pod.n!==336) throw new Error('podcasts='+(pod&&pod.n));
if(ACESSIBILIDADE_DAS_MIDIAS.length!==8) throw new Error('dimensoes: '+ACESSIBILIDADE_DAS_MIDIAS.length);
const lib = ACESSIBILIDADE_DAS_MIDIAS.find((d:any)=>d.campo==='libras');
if(lib.n!==3||lib.de!==529) throw new Error('libras '+lib.n+'/'+lib.de);
const zeradas = ACESSIBILIDADE_DAS_MIDIAS.filter((d:any)=>d.n===0);
if(zeradas.some((d:any)=>d.sustentada)) throw new Error('dimensao zerada marcada sustentada');
if(PONTE_COM_EVENTO.arestas!==34||PONTE_COM_EVENTO.midiasDistintas!==14||PONTE_COM_EVENTO.deQuantas!==529) throw new Error('ponte: '+JSON.stringify(PONTE_COM_EVENTO));
if(!/\d/.test(PONTE_COM_EVENTO.declaracao)) throw new Error('declaracao sem numero');
if(!itemDoPlay(c[0].slug)) throw new Error('itemDoPlay nao resolve');
const bytes=JSON.stringify(c).length; if(bytes>100*1024) throw new Error('catalogo '+bytes+' bytes, teto 102400');
console.log('OK 529 itens, '+CATEGORIAS.length+' categorias, libras 3/529, ponte 34/14/529, '+bytes+' bytes');
"</automated>
  </verify>
  <done>O módulo devolve as 529 com categorias somando 529, as 8 dimensões contadas com as zeradas marcadas não-sustentadas, a ponte com evento medida em 34/14/529 com declaração numerada, e um DTO abaixo de 100 KB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /play — o catálogo unificado, o filtro de acessibilidade honesto e «continue de onde parou»</name>
  <files>src/componentes/play.tsx, src/app/(app)/play/page.tsx, src/estilos/play.css</files>
  <behavior>
    - A tela mostra `data-midia` para os itens do recorte corrente e declara o total de 529.
    - Há um controle `data-categoria` por categoria presente, cada um com a sua contagem; clicar
      num deles recorta a lista sem navegar — a URL não muda.
    - O filtro de acessibilidade mostra Libras com «3 de 529» e as dimensões de contagem zero
      com `data-nao-sustenta` e o seu denominador, antes de qualquer marcação.
    - «Continue de onde parou» está vazio e explicado num navegador limpo; depois de o Player
      registrar uma conclusão, o item aparece ali, e continua aparecendo depois de recarregar.
    - O bloco `data-veja-isto` declara a cobertura real da ponte com evento, com o denominador,
      com o modo comentado DESLIGADO.
    - Nada corre para fora da moldura: `scrollWidth` é igual a `clientWidth`.
  </behavior>
  <action>
    `page.tsx` é de servidor e é ele que toca `play.ts`; o componente de cliente recebe o DTO
    por propriedade e não alcança `@/dados/grafo` nem transitivamente (DP-F).

    **O catálogo é unificado (tela 19): uma lista só, com o tipo etiquetado**, e não uma
    prateleira por categoria. As categorias são recorte, não seções — é isso que faz o catálogo
    ser unificado em vez de quatro catálogos lado a lado. Cada item mostra a categoria em
    português, a capa (`<CapaSemImagem>` para as 11 sem imagem), o resumo, a data de publicação
    e a procedência.

    **O filtro de acessibilidade é onde esta tela pode mentir mais fácil.** A tela 19 pede
    legenda, Libras e audiodescrição como se fossem três recortes equivalentes; medido, um
    recorta 3 de 529 e os outros dois recortam zero. Mostre os três **com o número ao lado do
    rótulo, antes de marcar** — «Libras · 3 de 529», «audiodescrição · 0 de 529» — e deixe os
    três marcáveis, com o vazio explicado. É a mesma disciplina de `/filtros/`, e as duas telas
    têm de contar a mesma história.

    **«Continue de onde parou».** Leia a chave que o Player grava — escolha o nome dentro do
    espaço `agenda-cultural:`, declare-o no SUMMARY, e **não toque em
    `src/contexto/sessao.tsx`**. Valor que não é lista devolve lista vazia, no molde de
    `lerLista` de `sessao.tsx`; id que não resolve no catálogo é descartado em silêncio e o
    descarte é declarado, como a prévia de impacto da fase 4 declara o dela. Num navegador
    limpo o bloco aparece **vazio e explicado**, não sumido.

    **O bloco «não pode ir? veja isto» (D-92)** declara a cobertura real: quantas das 529 têm
    ligação com evento, quantos eventos elas alcançam, e por que o número é esse. Fora de
    `<Comentario>` — é produto. Onde houver ligação, mostre-a com o link para o evento.

    Ordenação determinística, nunca `localeCompare`: por data de publicação decrescente e
    desempate por chave estável.

    **O peso é o risco.** Se o DTO estourar 100 KB, o corte é de campo — resumo truncado no
    catálogo — e o corte fica declarado na tela. Não corte itens: 529 é o número que a proposta
    afirma.

    A tela é da superfície app e vive dentro da moldura, com barra de abas: «cabe na primeira
    vista» desconta os 59 px da barra grudada. Grade de web em `play.css` sob
    `[data-view="web"]`, consumindo `.web-grade` de `web.css` sem editá-la.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-07.ts play</automated>
  </verify>
  <done>No DOM vivo: catálogo unificado declarando 529, um controle por categoria recortando sem navegar, o filtro de acessibilidade mostrando 3 de 529 em Libras e zero declarado nas demais, «continue de onde parou» vazio e explicado num navegador limpo, e o bloco de ponte com evento declarando a cobertura com o modo comentado desligado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /play/[slug] — o Player com acessibilidade em evidência, a ausência do arquivo declarada, e o registro no repertório ao concluir</name>
  <files>src/componentes/player.tsx, src/app/(app)/play/[slug]/page.tsx</files>
  <behavior>
    - Existem 529 rotas em `out/play/`, uma por mídia.
    - A tela tem `data-player`, mostra a ficha das 8 dimensões e o crédito da imagem.
    - `data-sem-arquivo` está presente e visível com o modo comentado DESLIGADO, com a
      explicação de por que o protótipo não carrega mídia.
    - Antes de qualquer clique, `data-assistido` mede zero para esta mídia.
    - Clicar em `data-concluir` faz `data-assistido` passar a um, e o registro sobrevive a
      recarregar a página.
    - Abrir uma segunda mídia e concluí-la deixa duas registradas, e concluir a mesma duas
      vezes deixa uma — o registro é um conjunto, idempotente.
    - Zero requisição para fora do servidor local durante toda a sessão.
  </behavior>
  <action>
    `page.tsx` é de servidor, com `generateStaticParams` sobre `slugsPorTipo("midia")` e a
    reserva para classe vazia no molde das rotas irmãs. **São 529 páginas novas** — o maior
    acréscimo da fase. Mantenha o DTO por página enxuto; ele não paga chunk, mas paga HTML.

    **O Player não toca mídia, e isso não é um defeito escondido: é a tela dizendo o que é.**
    O acervo tem a ficha e a capa, não o arquivo, e `fonte` aponta para fora — buscar de lá
    quebraria a promessa medida de zero requisição externa. Ponha `data-sem-arquivo` com a
    frase que explica as duas coisas: que o acervo carregado não traz o arquivo, e que o
    protótipo não faz requisição para fora por decisão. Mostre `fonte` como link declarado,
    para quem quiser ir ver — **um link que a pessoa clica não é uma requisição que o protótipo
    faz**, e a diferença é exatamente o que o gate mede. Não carregue nada de lá: nem `<img>`,
    nem `<iframe>`, nem `fetch`, nem `<video>` com `src` remoto, nem `preconnect`.

    **Os controles de acessibilidade em evidência (tela 20).** Legenda, Libras e audiodescrição
    aparecem como controles de primeira ordem, cada um mostrando se ESTA mídia declara aquela
    dimensão — e, quando não declara, se é declarado-ausente ou não-declarado (D-43).
    `ficha-acessibilidade.tsx` já tem esse vocabulário: reaproveite-o em vez de escrever outro.

    **O registro no repertório ao concluir (D-92).** Um controle `data-concluir` visível, que
    grava numa chave nova do espaço `agenda-cultural:` — a mesma que `play.tsx` lê em «continue
    de onde parou». **Não toque em `src/contexto/sessao.tsx`.** O registro é um CONJUNTO, não
    uma pilha: concluir duas vezes a mesma mídia deixa uma entrada. Valor que não é lista no
    storage devolve lista vazia e a tela continua de pé, no molde de `lerLista` de
    `sessao.tsx`.

    **A conclusão é um gesto humano, nunca automática.** Nada de temporizador, nada de gravar
    ao abrir. `data-assistido` mede zero até o clique — é a mesma disciplina do «zero decisão
    antes do clique» que a fase 4 fixou no Studio, e o gate a mede do mesmo jeito.

    **O que aprofunda aquilo** (tela 20): as arestas `aprofunda` e `fala_sobre` que saem desta
    mídia, com o destino nomeado. Onde não houver nenhuma — e serão a maioria —, declare com o
    denominador em vez de sumir com o bloco.

    Carimbe a conclusão com a **data de referência do build**, jamais com o relógio do runtime:
    sob export estático um relógio de runtime faz o HTML exportado e a hidratação divergirem e
    vaza o fuso de quem avalia. É o mesmo padrão de `DATA_DE_REFERENCIA_DO_STUDIO`.

    Compare data por número, nunca por string entre `DD.MM.AAAA` e ISO — a comparação entre os
    dois formatos está quebrada e já custou tempo nesta obra.
  </action>
  <verify>
    <automated>npm run build && node -e "
const {readdirSync,readFileSync,existsSync}=require('fs');
if(!existsSync('out/play')) throw new Error('out/play ausente');
const rotas=readdirSync('out/play',{withFileTypes:true}).filter(e=>e.isDirectory()).length;
if(rotas!==529) throw new Error('rotas em out/play: '+rotas+' — esperado 529');
const primeira=readdirSync('out/play',{withFileTypes:true}).filter(e=>e.isDirectory())[0].name;
const h=readFileSync('out/play/'+primeira+'/index.html','utf8');
for(const a of ['player','sem-arquivo','concluir']) if(!new RegExp('data-'+a+'=\"').test(h)) throw new Error('falta data-'+a);
if((h.match(/data-assistido=\"1\"/g)||[]).length) throw new Error('data-assistido=1 no HTML exportado; conclusao e gesto humano');
if(/<iframe|<video[^>]+src=\"https?:|<audio[^>]+src=\"https?:|rel=\"preconnect\"/.test(h)) throw new Error('carregamento remoto no HTML do player');
console.log('OK 529 rotas de player, sem carregamento remoto, 0 assistido no artefato');
" && node_modules/.bin/tsx scripts/sonda-05-07.ts player && npm run verificar-fase2 && npm run verificar-fase4</automated>
  </verify>
  <done>529 rotas de player existem, nenhuma carrega recurso remoto, `data-assistido` é zero no artefato e passa a um só depois do clique humano, o registro sobrevive a recarregar e é idempotente, e as suítes 2 e 4 seguem verdes.</done>
</task>

</tasks>

<sonda_do_plano>
As tasks 2 e 3 pedem `scripts/sonda-05-07.ts`, sonda descartável — **apague antes do último
commit** e confirme `git status --short scripts/` vazio. A suíte permanente é de 05-08.

API real de `scripts/navegador.mjs`: `servir({raiz})` de `servir-out.mjs` devolve
`{url, fechar}`; `abrirNavegador()` devolve o `cdp` diretamente; o método é `cdp.navegar(url)`;
console em `cdp.consola`; **`cdp.clicar` recebe uma EXPRESSÃO JavaScript que avalia para o
elemento**, não um seletor CSS; `cdp.avaliar(expressao)` e `naPagina(corpo)` embrulham com o
preludio. **Não existe `Network.*`** — a prova de zero requisição externa é
`performance.getEntriesByType('resource')` de dentro da página, e **é o gate mais importante
deste plano**: filtre por origem e exija que toda entrada esteja no servidor local.
`visiveis()` falha sobre SVG.

**«Cabe na primeira vista» desconta os 59 px da barra de abas grudada** — `/play/` e
`/play/[slug]/` estão no grupo `(app)`.

`data-assistido` é atributo de interação: mede zero no HTML exportado de propósito, e o valor
após conclusão só existe no DOM vivo, depois do clique. Meça-o nas duas formas e documente o
zero, como 04-05 fez com os seis atributos de interação do Studio.

Para provar a sobrevivência a recarregar, use `cdp.recarregar()` e espere `data-hidratado="sim"`
antes de medir de novo — o valor inicial do contexto é o do HTML estático, não o do storage.

Exercite o storage adulterado: chave com valor que não é lista, e id que não resolve no
catálogo. A tela tem de continuar de pé nos dois casos, e o descarte tem de ser declarado.

**Onde o gate afirmar usabilidade, meça geometria contra o contêiner** — `scrollWidth` contra
`clientWidth`, retângulo do controle de concluir contra o limite útil. Presença e visibilidade
já deram verde sobre tela quebrada duas vezes nesta obra.

Defina `DIR_CAPTURAS` e guarde fotos **fora do repositório** de `/play/` e de um player, para
julgamento humano — um catálogo de 529 itens é onde uma grade quebrada passa despercebida em
qualquer contagem.
</sonda_do_plano>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefato → internet | `fonte` de cada mídia aponta para itaucultural.org.br; qualquer carregamento de lá quebra a promessa medida |
| localStorage → tela | a chave de conclusão é editável pelo avaliador e não é confiável |
| build → cliente | `play.ts` alcança 23 MB de grafo e não pode ser importado por valor do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-33 | Information disclosure | `<iframe>`, `<video src>` remoto ou `preconnect` para o site do IC | critical | mitigate | proibidos no plano; gate varre o HTML exportado e o gate de rede mede por dentro da página |
| T-05-34 | Spoofing | autorar aresta mídia→evento para inflar «não pode ir? veja isto» | critical | mitigate | proibido; a ponte é 34 arestas de 14 mídias e o bloco declara 14 de 529 |
| T-05-35 | Spoofing | oferecer legenda, Libras e audiodescrição como recortes equivalentes | high | mitigate | número ao lado de cada rótulo antes de marcar; gate exige `data-nao-sustenta` nas zeradas |
| T-05-36 | Elevation of privilege | conclusão automática por temporizador registrando sem gesto humano | high | mitigate | proibido; gate exige `data-assistido` zero antes do clique e um depois |
| T-05-37 | Tampering | storage adulterado quebrando a tela ou inflando o registro | medium | mitigate | valor que não é lista devolve vazio; id não resolvido é descartado e o descarte é declarado; gate exercita os dois |
| T-05-38 | Denial of service | catálogo de 529 estourando o teto de 1.600 KB de chunks | high | mitigate | teto de 100 KB medido na Task 1; corte de campo, nunca de item, com o corte declarado na tela |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **este plano não instala pacote nenhum**; nenhum reprodutor de mídia de terceiros entra |
</threat_model>

<verification>
1. `npm run build` compila e exporta; `out/play/` tem 529 rotas. **Registre no SUMMARY o total
   de páginas medido e as 529 rotas** — 05-08 precisa dos dois para a lista de explicáveis do
   gate da fase 3, sem mover o limiar de 1.784. **Não afirme total absoluto no gate**: outro
   plano da onda também acrescenta rota.
2. `npm run verificar-fase2` e `verificar-fase4` continuam verdes — em especial o teto de
   chunks de 1.600 KB, que este plano é o mais capaz de estourar.
3. `npm run verificar-comentado` verde: `data-sem-arquivo` e a declaração da ponte não podem
   sumir com o modo comentado desligado.
4. `verificar-fase3` vermelha só no gate 8 de `globals.css`, herdado de 05-01.
5. Console sem erro nem aviso da aplicação; **zero requisição externa em toda a sessão**.
6. `git diff --stat` não mostra `globals.css`, `web.css` nem `sessao.tsx`.
</verification>

<success_criteria>
- Catálogo unificado das 529, recortável pelas categorias reais, sem navegar.
- Filtro de acessibilidade com o número ao lado de cada rótulo e as zeradas declaradas.
- «Não pode ir? veja isto» declarando 14 de 529 e os eventos alcançados.
- 529 rotas de player, nenhuma carregando recurso remoto.
- Conclusão só por gesto humano, sobrevivendo a recarregar, idempotente.
- `data-sem-arquivo` explicando a ausência do arquivo e a decisão de não buscar fora.
- Peso de chunks cresce no máximo 100 KB e o total continua abaixo de 1.600 KB.
- Total de páginas e contagem de rotas de player registrados no SUMMARY.
- A sonda descartável foi apagada.
</success_criteria>

<output>
Create `.planning/phases/05-camada-2-web-desktop-e-profundidade/05-07-SUMMARY.md` when done
</output>
