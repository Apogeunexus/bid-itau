---
phase: 05-camada-2-web-desktop-e-profundidade
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(app)/evento/[slug]/page.tsx
  - src/app/(app)/produtor/[slug]/page.tsx
  - src/componentes/produtor.tsx
  - src/estilos/web-evento.css
  - src/estilos/produtor.css
autonomous: true
requirements: [WEB-03, APPX-05]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Na visão web, a página do evento mostra as ocorrências em TABELA com a acessibilidade em coluna, e «aprofunda isto» em painel lateral em vez de bloco no pé (D-80, tela 27)"
    - "Na visão app, a página do evento continua exatamente como a fase 2 a deixou, com os mesmos blocos na mesma ordem"
    - "A página do produtor mostra território, linguagens em que atua, o que realiza, pessoas ligadas e o que fala sobre — com cada bloco vazio se declarando por frase com denominador, nunca sumindo (D-90)"
    - "«O que está em cartaz agora» declara, com o denominador medido, que nenhum dos 359 produtores realiza evento datado — e diz por quê"
  artifacts:
    - src/componentes/produtor.tsx
    - src/estilos/web-evento.css
    - src/estilos/produtor.css
  key_links:
    - "evento/[slug]/page.tsx → .web-duas-colunas e .web-painel de web.css (escritas por 05-01)"
    - "produtor.tsx → vizinhos(realiza|situado_em|pertence_a|fala_sobre) de grafo.ts e vinculosDe de ponte.ts"
    - "produtor.tsx → BlocoAusenciaDeclarada de ponte.ts, a frase de ausência que a fase 2 já fixou"
---

<objective>
A ficha do evento ganha densidade de desktop — tabela e painel lateral em vez de blocos
empilhados — e a página do produtor deixa de ser esqueleto e passa a existir como entidade
viva do grafo.

Purpose: WEB-03 é a tela em que a diferença entre mobile esticado e layout próprio fica mais
óbvia para quem avalia: 53 sessões em pilha vertical são inutilizáveis, 53 sessões em tabela
com acessibilidade em coluna são um produto. E APPX-05 é o fluxo obrigatório do RFP — a página
do produtor é citada nominalmente — que hoje responde com um esqueleto.

Output: `/evento/[slug]/` com layout de desktop na visão web e `/produtor/[slug]/` preenchida
nas 359 rotas que já existem, sem acrescentar uma página ao artefato.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/5-CONTEXT.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/05-01-SUMMARY.md
@src/app/(app)/evento/[slug]/page.tsx
@src/app/(app)/produtor/[slug]/page.tsx
@src/componentes/lista-ocorrencias.tsx
@src/componentes/ponte.tsx
@src/componentes/tela-entidade.tsx
@src/componentes/verbete.tsx
@src/dados/ponte.ts
@src/estilos/web.css
</context>

<contrato_herdado>
**Leia `05-01-SUMMARY.md` antes de escrever uma linha.**

1. **`src/estilos/web.css` já existe e está escrita** — `.web-duas-colunas`, `.web-painel`,
   `.web-grade`, `.web-coluna-fixa`, `.web-realce`, todas sob `[data-view="web"]`. **Consuma;
   não edite.**
2. **`src/estilos/web-evento.css` e `src/estilos/produtor.css` já existem, vazias de regra.**
   São suas. **Não toque em `src/app/globals.css`** — os 21 `@import` já estão declarados e há
   gate herdado que reprova regra acrescentada ao arquivo.
3. Deste plano são os atributos `data-tabela-ocorrencias`, `data-coluna-acessibilidade` e
   `data-painel-aprofunda`. Não emita atributo de outro plano.

**Orçamento de chunks deste plano: 20 KB.**

`npm run verificar-fase3` está vermelha no gate 8 (`globals.css` ancorado em `c03f627`) desde
o commit de 05-01. É esperado, o reancoramento é de 05-08, **não conserte aqui**. Para
regressão use `verificar-fase2` e `verificar-fase4`.
</contrato_herdado>

<protocolo_de_disco>
Volume a 96% com histórico de despejo do iCloud — `stat` informa tamanho, a leitura devolve
zero byte. Confirme cada arquivo existente com `wc -c` contra `git show HEAD:<arquivo> | wc -c`
**antes de editar**; divergiu ou leu zero, restaure com `git show HEAD:<arquivo> > <arquivo>`,
nunca com `brctl download`. Antes de cada commit leia `git diff --cached --numstat`; depois
confira os bytes NO GIT e leia `git diff-tree --name-status -r HEAD`. Empurre com
`git push espelho HEAD`. Conferir o STATE.md depois de qualquer `state.update-progress`.
</protocolo_de_disco>

<o_que_foi_medido_antes_de_planejar>
Apurado contra o grafo em disco durante o planejamento. Confirme e siga; não redescubra.

- **359 produtores** — 246 instituições + 113 espaços. As 359 rotas de `/produtor/[slug]/` **já
  existem no artefato** e estão dentro da linha de base de 1.784 páginas: preencher esta tela
  **não acrescenta página nenhuma**.
- **327** têm território por `situado_em`; **303** têm linguagem por `pertence_a`.
- **527 arestas `realiza`** saem de produtores para eventos, e alcançam **127** produtores
  distintos. Os outros 232 não realizam nada no acervo.
- **0 produtores realizam evento DATADO.** Nenhum dos 527 eventos realizados tem ocorrência.
  É a mesma lacuna que a fase 2 registrou por escrito: dos 129 eventos com ocorrência, zero
  têm aresta de agente; dos 67 com agente, zero têm ocorrência.
- **101 eventos e 101 temporadas** estão situados em algum produtor por `situado_em`.
- Produtores não têm aresta `atua_em`. Pessoas ligadas a um produtor só existem por travessia
  de dois saltos, através dos eventos que ele realiza. **Meça quantos produtores têm ao menos
  uma pessoa alcançável assim antes de escrever a frase**, e escreva a frase com o número.

**A consequência de projeto.** «O que está em cartaz agora» é bloco obrigatório da tela 24 e o
acervo não o sustenta em nenhum dos 359 casos. Ele **não pode sumir** e **não pode aparecer
zerado sem explicação** (D-90): aparece com a frase que nomeia o denominador, diz que os
eventos realizados são todos da Enciclopédia e por isso históricos, e aponta que programação
futura é exatamente o que chega quando os produtores publicarem no Studio. Não autore aresta
para tapar o buraco — dizer que a instituição X realiza o evento Y em data Z seria alegação
factual falsa sobre organização real.
</o_que_foi_medido_antes_de_planejar>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A página do evento na visão web — ocorrências em tabela, acessibilidade em coluna, «aprofunda isto» em painel lateral</name>
  <files>src/app/(app)/evento/[slug]/page.tsx, src/estilos/web-evento.css</files>
  <precondition>`src/app/(app)/evento/[slug]/page.tsx` e `src/estilos/web-evento.css` leem e batem com o git antes da primeira edição.</precondition>
  <behavior>
    - Na visão web, num evento com muitas sessões, o bloco de ocorrências tem
      `data-tabela-ocorrencias` e cada linha tem uma célula `data-coluna-acessibilidade`.
    - O painel `data-painel-aprofunda` está visível na primeira vista da visão web — o topo do
      painel fica acima da base da moldura sem rolar.
    - O painel lateral continua dentro da janela depois de rolar a página: ele é `sticky`.
    - A tabela não corre para fora da janela: `scrollWidth` do contêiner é igual ao seu
      `clientWidth`.
    - Na visão app, os mesmos blocos aparecem na mesma ordem da fase 2 e NENHUM elemento
      carrega `data-tabela-ocorrencias`.
  </behavior>
  <action>
    A divergência é de layout, dentro do mesmo arquivo (D-79, D-05). É proibido criar
    `EventoWeb`. `page.tsx` continua sendo componente de servidor, continua resolvendo espaço
    pelos três caminhos que a fase 2 fixou, e continua declarando ausência pela frase de
    `ponte.ts` em vez de sumir com o bloco.

    O invólucro do conteúdo ganha `.web-duas-colunas` de `web.css`: coluna principal com
    verbete, ficha e ocorrências; coluna lateral, colável, com «aprofunda isto» e os vínculos
    de `vinculosDe`. Em `[data-view="mobile"]`, `web-evento.css` devolve as duas colunas ao
    empilhamento da fase 2, na ordem em que elas já estavam — **a ordem do DOM é uma só**, e
    quem reordena é o layout, nunca um ramo em JavaScript.

    `<ListaDeOcorrencias>` é da fase 2 e serve as duas visões: **não a duplique**. Na visão
    web, sob `[data-view="web"]` em `web-evento.css`, as suas linhas viram grade de tabela com
    cabeçalho — data, horário, temporada, acessibilidade — e a célula de acessibilidade recebe
    `data-coluna-acessibilidade`. Se `lista-ocorrencias.tsx` não emitir hoje os ganchos de
    classe necessários para isso, acrescente-os LÁ apenas como atributo e classe semântica,
    sem alterar comportamento nem props — e registre no SUMMARY que o arquivo foi tocado, para
    05-08 medir que a visão app não mudou. Prefira resolver só com CSS se der.

    **A tabela é o ponto em que esta tela justifica a visão web** (tela 27). Um evento do CMS
    tem 53 sessões; empilhadas verticalmente elas são uma rolagem, em tabela com coluna de
    acessibilidade elas são consultáveis. Meça: nenhuma linha pode transbordar a moldura, e o
    cabeçalho da tabela fica grudado ao topo da tabela ao rolar.

    A ficha de acessibilidade continua distinguindo declarado-ausente de não-declarado, que é
    a leitura de `declaraAcessibilidade` que D-43 fixou — não colapse os dois em «não tem».

    **Duas páginas muito diferentes saem deste mesmo arquivo**, e isso é medido: o evento da
    Enciclopédia tem arestas de atuação e realização e nenhuma ocorrência datada; o do CMS tem
    sessões datadas e zero agentes. Teste as duas na sonda, não só uma — um layout de duas
    colunas em que a lateral fica vazia numa das metades é exatamente o defeito que a ausência
    declarada existe para impedir.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-03.ts evento</automated>
  </verify>
  <done>Nas duas páginas de exemplo — uma da Enciclopédia e uma do CMS — a visão web mostra tabela com coluna de acessibilidade e painel lateral colável dentro da janela, e a visão app continua com os mesmos blocos na mesma ordem da fase 2; console sem erro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: produtor.tsx — a entidade viva, com cada bloco vazio se declarando pelo número</name>
  <files>src/componentes/produtor.tsx, src/estilos/produtor.css</files>
  <behavior>
    - Para um produtor com território, a tela mostra o território nomeado e a via pela qual ele
      foi resolvido.
    - Para um produtor que realiza eventos, a tela lista os eventos realizados com a contagem.
    - «O que está em cartaz agora» aparece SEMPRE, com `data-nao-sustenta`, com o denominador
      medido, e com altura maior que zero com o modo comentado DESLIGADO.
    - Para um produtor sem linguagem declarada, o bloco de linguagens aparece com a frase de
      ausência de `ponte.ts` em vez de sumir.
    - Nenhum bloco da tela desaparece por estar vazio: os blocos previstos pela tela 24 estão
      todos presentes em todos os produtores.
  </behavior>
  <action>
    Crie `src/componentes/produtor.tsx`. Ele recebe por propriedade um DTO só de primitivo,
    montado na página de servidor — nada aqui alcança `@/dados/grafo`, nem transitivamente
    (DP-F). Se ele não precisar de estado nem de evento, **não ponha `"use client"`**: um
    componente de servidor a menos no gate de fronteira é um risco a menos.

    Os blocos, exatamente os da tela 24 de `docs/telas.md`, nesta ordem: nome, imagem e
    procedência · território e espaço · o que está em cartaz agora · histórico · pessoas
    ligadas · linguagens em que atua · conteúdo editorial que fala sobre.

    **Nenhum bloco pode sumir por estar vazio.** Reaproveite `<BlocoAusenciaDeclarada>` de
    `ponte.ts`/`ponte.tsx`, que é o mecanismo que a fase 2 já pagou para isso, e dê a cada
    ausência o seu denominador medido — não uma frase genérica. «Este produtor não declara
    linguagem; 303 dos 359 declaram» diz mais e mente menos que «sem linguagens».

    O bloco **«o que está em cartaz agora»** é o caso duro. Ele leva `data-nao-sustenta` e a
    frase composta com os números medidos: quantos produtores realizam evento, quantos
    realizam evento datado, e por que o segundo número é o que é. Ele **fica fora de
    `<Comentario>`** — é produto, não nota de protótipo, e tem de continuar na tela com o modo
    comentado desligado, como as declarações de honestidade da fase 4.

    **Pessoas ligadas** existem por travessia de dois saltos: as pessoas que atuam nos eventos
    que este produtor realiza. Meça quantos dos 359 têm ao menos uma pessoa alcançável assim e
    ponha o número na frase de ausência de quem não tem. Traga o papel da relação `atua_em`
    junto do nome — o papel mora na aresta e é o que faz a ligação ser nomeada em vez de um
    link solto (DADO-03, D-40).

    Reaproveite `<CapaSemImagem>`, `<SelosDeLinguagem>` e `<Verbete>`. **Não use `<Cartao>`**
    para os eventos realizados: o motivo de um cartão é o texto de uma aresta `semelhante_a`, e
    «realiza» não é essa aresta — carimbar um motivo composto afirmaria uma relação que o grafo
    não tem.

    A tela é mobile (APPX-05, superfície app). Ela também aparece na visão web dentro da
    moldura larga; dê a ela um layout de duas colunas sob `[data-view="web"]` em
    `produtor.css` se couber, mas **a visão web da página do produtor é Camada 3 e não é
    escopo desta fase** — não invista nela além de não deixá-la feia.

    Nada de HTML bruto: `resumo` já vem como texto puro do gerador e assim continua.
  </action>
  <verify>
    <automated>node_modules/.bin/tsx -e "
import {porSlug, slugsPorTipo, vizinhos} from '@/dados/grafo';
const inst = slugsPorTipo('instituicao'), esp = slugsPorTipo('espaco');
if (inst.length + esp.length !== 359) throw new Error('produtores: ' + (inst.length+esp.length));
let comTerr=0, comLing=0, comRealiza=0;
for (const s of inst.concat(esp)) {
  const e = porSlug('instituicao', s) ?? porSlug('espaco', s); if(!e) continue;
  if (vizinhos(e.id,'situado_em').length) comTerr++;
  if (vizinhos(e.id,'pertence_a').some((v:any)=>v.entidade.classe==='linguagem')) comLing++;
  if (vizinhos(e.id,'realiza').length) comRealiza++;
}
console.log('OK medido: territorio='+comTerr+' linguagem='+comLing+' realiza='+comRealiza);
if (comRealiza !== 127) throw new Error('realiza esperado 127, medido '+comRealiza);
"</automated>
  </verify>
  <done>`produtor.tsx` existe, monta os sete blocos da tela 24 sem que nenhum suma, e os números que ele declara batem com a medição contra o grafo.</done>
</task>

<task type="auto">
  <name>Task 3: /produtor/[slug] deixa de ser esqueleto nas 359 rotas</name>
  <files>src/app/(app)/produtor/[slug]/page.tsx</files>
  <behavior>
    - As 359 rotas continuam sendo exportadas: `generateStaticParams` devolve exatamente os
      mesmos slugs de antes e **este plano não acrescenta rota nenhuma ao artefato**.
    - Uma instituição com território e uma sem, e um espaço, abrem sem erro de console.
    - Nenhum `<TelaEntidade>` sobra na rota.
  </behavior>
  <action>
    Troque o `<TelaEntidade>` esqueleto pelo `<Produtor>` da Task 2. `generateStaticParams`
    continua exatamente como está — `slugsPorTipo("instituicao")` mais `slugsPorTipo("espaco")`,
    com a reserva para classe vazia — porque mexer nele mudaria o total de páginas de `out/` e
    a linha de base de 1.784 do gate da fase 3.

    Toda a travessia acontece AQUI, no build: `vizinhos(id, "situado_em")` para território,
    `vizinhos(id, "realiza")` para o que realiza, `vizinhos(id, "pertence_a")` filtrado por
    classe `linguagem`, `vizinhos(id, "fala_sobre")` invertido para o conteúdo editorial que
    fala sobre ele, e a travessia de dois saltos para pessoas ligadas. O que atravessa a
    fronteira RSC é o DTO de primitivos (D-47: nunca varredura de array cru; sempre as funções
    públicas de `grafo.ts`).

    **Meça o custo antes de commitar.** 359 páginas × um DTO gordo é como se estoura um teto:
    corte campo, não qualidade, se o peso de `out/_next/static/chunks` subir mais de 20 KB. Se
    um produtor realiza dezenas de eventos, limite a lista exibida e declare o total — mostrar
    12 de 47 com o número visível é honesto; mostrar 47 e estourar o orçamento não é.

    Confira que a página resolve tanto `instituicao` quanto `espaco` pelo mesmo slug, como o
    arquivo já faz, e que um slug inexistente cai na frase de entidade ausente em vez de
    estourar.
  </action>
  <verify>
    <automated>npm run build && node -e "
const {execSync}=require('child_process');
const {readdirSync,readFileSync}=require('fs');
// NAO se afirma total absoluto: outros planos desta onda acrescentam rota em paralelo e o
// numero de out/ depende de quem ja aterrissou. Afirma-se o que E deste plano.
const dirs=readdirSync('out/produtor',{withFileTypes:true}).filter(e=>e.isDirectory()).map(e=>e.name);
if(dirs.length!==359) throw new Error('rotas de produtor: '+dirs.length+' — esperado 359, e este plano nao pode mudar esse numero');
let semSustenta=0;
for(const d of dirs.slice(0,40)){
  const h=readFileSync('out/produtor/'+d+'/index.html','utf8');
  if(/TelaEsqueleto/.test(h)) throw new Error('esqueleto ainda na rota '+d);
  if(!/data-nao-sustenta=\"/.test(h)) semSustenta++;
}
if(semSustenta) throw new Error(semSustenta+' de 40 rotas sem bloco de ausencia declarada');
const total=Number(execSync('find out -name \"*.html\" -not -path \"out/_next/*\" -not -path \"out/acervo/*\" | wc -l').toString().trim());
console.log('OK 359 rotas de produtor, sem esqueleto · total de paginas medido agora: '+total+' — REGISTRE ESTE NUMERO NO SUMMARY para 05-08');
" && npm run verificar-fase2 && npm run verificar-fase4</automated>
  </verify>
  <done>As 359 rotas de produtor respondem com a tela real, este plano não acrescenta rota nenhuma, o total de páginas medido está registrado no SUMMARY para 05-08, e as suítes 2 e 4 seguem verdes.</done>
</task>

</tasks>

<sonda_do_plano>
As tasks 1 e 3 pedem `scripts/sonda-05-03.ts`, sonda descartável — **apague antes do último
commit** e confirme `git status --short scripts/` vazio. A suíte permanente é de 05-08.

API real de `scripts/navegador.mjs`: `servir({raiz})` de `servir-out.mjs` devolve
`{url, fechar}`; `abrirNavegador()` devolve o `cdp` diretamente; o método é `cdp.navegar(url)`;
console em `cdp.consola`; `cdp.clicar` recebe EXPRESSÃO JavaScript que avalia para o elemento,
não seletor CSS; `cdp.avaliar(expressao)` e `naPagina(corpo)` embrulham com o preludio. **Não
existe `Network.*`** — prove rede por `performance.getEntriesByType('resource')` de dentro da
página. `visiveis()` falha sobre SVG.

Visão web por `localStorage.setItem("agenda-cultural:visao","web")` (string crua, não JSON)
mais recarregar; espere `data-hidratado="sim"` antes de medir.

**«Cabe na primeira vista» mede contra a moldura menos a barra de abas grudada de 59 px** — a
página do evento e a do produtor estão no grupo `(app)` e recebem a barra. Não reaproveite a
medida do bastidor, que não monta barra.

**Onde o gate afirmar usabilidade, meça geometria**: retângulo do painel lateral contra o da
moldura, `scrollWidth` contra `clientWidth` da tabela, e a posição do painel depois de rolar.
Presença e visibilidade já deram verde sobre tela quebrada duas vezes nesta obra. Defina
`DIR_CAPTURAS` e guarde fotos das duas telas nas duas visões, **fora do repositório**, para
julgamento humano.

Compare data por número, nunca por string: o projeto exibe `DD.MM.AAAA` e guarda ISO, e a
comparação de string entre os dois formatos está quebrada.
</sonda_do_plano>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → artefato | 359 páginas de produtor + 300 de evento saem estáticas; DTO gordo vira peso permanente |
| grafo → tela | toda leitura passa pelas funções públicas de `grafo.ts` (D-47), nunca por array cru |
| fonte → HTML | `resumo` e `creditoImagem` são texto puro; nenhum HTML bruto atravessa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Spoofing | autorar aresta produtor→evento datado para preencher «em cartaz» | critical | mitigate | proibido no plano; o bloco declara a ausência com denominador e nomeia a lacuna como o que o Studio fecha |
| T-05-10 | Tampering | `generateStaticParams` alterado mudando o total de páginas | high | mitigate | gate da Task 3 exige exatamente 1.931 páginas em `out/` |
| T-05-11 | Information disclosure | DTO do produtor carregando entidade inteira para o cliente | high | mitigate | DTO só de primitivo montado no servidor; gate DP-F transitivo herdado de 04-05 |
| T-05-12 | Denial of service | tabela de 53 sessões transbordando a moldura | high | mitigate | gate compara `scrollWidth` com `clientWidth` e exige captura de tela |
| T-05-13 | Elevation of privilege | HTML bruto vindo de `resumo` da fonte | medium | mitigate | gate herdado «inserção de HTML bruto em src/: 0 ocorrências» continua valendo |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **este plano não instala pacote nenhum**; zero dependência nova |
</threat_model>

<verification>
1. `npm run build` compila e exporta. **Este plano não acrescenta rota nenhuma** — as 359 de
   produtor e as 300 de evento já existiam. Não afirme total absoluto de páginas: outros planos
   desta onda acrescentam rota em paralelo. **Meça o total e registre-o no SUMMARY** para
   05-08.
2. `npm run verificar-fase2` verde: a página do evento é medida por gates da fase 2 e nenhum
   deles pode ser relaxado.
3. `npm run verificar-fase4` verde.
4. `npm run verificar-comentado` verde: os blocos de ausência declarada não podem sumir com o
   modo comentado desligado.
5. `verificar-fase3` vermelha só no gate 8 de `globals.css`, herdado de 05-01.
6. Console sem erro nem aviso da aplicação; zero requisição externa.
7. `git diff --stat` não mostra `globals.css` nem `web.css`.
</verification>

<success_criteria>
- Evento na visão web: tabela de ocorrências com coluna de acessibilidade, painel lateral
  colável na primeira vista e depois de rolar, nada transbordando a moldura.
- Evento na visão app: idêntico ao da fase 2, provado por medição.
- As 359 rotas de produtor respondem com os sete blocos da tela 24, nenhum sumindo.
- «O que está em cartaz agora» declara a ausência com o denominador medido e sobrevive ao modo
  comentado desligado.
- Este plano não acrescenta rota: `out/produtor/` tem 359 e nenhum padrão novo aparece.
- Peso de chunks cresce no máximo 20 KB.
- A sonda descartável foi apagada.
</success_criteria>

<output>
Create `.planning/phases/05-camada-2-web-desktop-e-profundidade/05-03-SUMMARY.md` when done
</output>
