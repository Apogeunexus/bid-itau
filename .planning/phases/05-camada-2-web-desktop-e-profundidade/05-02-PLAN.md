---
phase: 05-camada-2-web-desktop-e-profundidade
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(app)/descobrir/page.tsx
  - src/componentes/feed.tsx
  - src/componentes/buscar.tsx
  - src/estilos/web-descobrir.css
  - src/estilos/web-buscar.css
autonomous: true
requirements: [WEB-01, WEB-04]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Na visão web, /descobrir é grade de várias colunas e o destaque curado ocupa mais colunas que os demais cartões (D-80, tela 25)"
    - "O selo de motivo de cada cartão está visível na visão web sem nenhum toque nem hover"
    - "Na visão web, /buscar mostra as facetas em coluna permanente à esquerda e os resultados em grade — não há tela separada de facetas (D-80, tela 28)"
    - "Na visão app, as duas telas continuam exatamente como a fase 2 e a fase 3 as deixaram"
    - "Nenhum componente irmão por visão foi criado: a divergência é de layout, dentro do mesmo arquivo (D-79, D-05)"
  artifacts:
    - src/estilos/web-descobrir.css
    - src/estilos/web-buscar.css
    - src/componentes/feed.tsx
    - src/componentes/buscar.tsx
  key_links:
    - "feed.tsx → .web-grade e .web-realce de web.css (escritas por 05-01, consumidas aqui sem editar)"
    - "buscar.tsx → .web-coluna-fixa de web.css e facetasDe() de indice.ts"
    - "buscar.tsx → /filtros/, a rota que 05-06 cria nesta mesma onda"
---

<objective>
As duas telas de Camada 1 que mais ganham com tela grande passam a ter layout próprio de
desktop: Descobrir vira grade com o destaque curado ocupando largura maior, e Buscar troca a
gaveta de facetas por uma coluna permanente.

Purpose: D-80 diz que o que muda na web não é largura, é densidade e simultaneidade. Em
Descobrir isso significa ver muitos motivos ao mesmo tempo — o selo laranja deixa de ser algo
que se descobre rolando e vira a textura da tela. Em Buscar significa ver o recorte e o
resultado juntos, que é a diferença entre filtrar e navegar.

Output: `/descobrir/` e `/buscar/` com layout de desktop na visão web, sem uma linha de
componente irmão e sem tocar no comportamento da visão app.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/5-CONTEXT.md
@.planning/phases/05-camada-2-web-desktop-e-profundidade/05-01-SUMMARY.md
@.planning/phases/03-camada-1-agenda-territorio-e-busca/03-04-SUMMARY.md
@src/componentes/feed.tsx
@src/componentes/cartao.tsx
@src/componentes/buscar.tsx
@src/estilos/web.css
@src/estilos/busca.css
</context>

<contrato_herdado>
**Leia `05-01-SUMMARY.md` antes de escrever uma linha.** Dele vêm três coisas de que este
plano depende e que ele não pode redefinir:

1. **`src/estilos/web.css` já existe e está escrita.** Ela traz `.web-grade`,
   `.web-coluna-fixa`, `.web-painel`, `.web-duas-colunas` e `.web-realce`, todas sob
   `[data-view="web"]`. **Consuma; não edite.** `web.css` pertence a 05-01 e outro executor
   desta onda pode estar lendo o mesmo arquivo.
2. **`src/estilos/web-descobrir.css` e `src/estilos/web-buscar.css` já existem, vazias de
   regra, com cabeçalho.** São suas. **Não toque em `src/app/globals.css`** — as 21 linhas de
   `@import` já estão lá, e há gate herdado que reprova qualquer regra acrescentada ao
   arquivo.
3. **O vocabulário `data-*` da fase 5, congelado.** Deste plano são `data-grade-web`,
   `data-destaque-curado` e `data-coluna-facetas`. Não emita atributo de outro plano.

**Orçamento de chunks deste plano: 20 KB** sobre os 1.065 KB medidos. É trabalho de layout;
nenhum DTO novo deveria atravessar a fronteira.

**`npm run verificar-fase3` está vermelha no gate 8** desde o commit de 05-01, por ter
`globals.css` ancorado em `c03f627`. É esperado, o reancoramento é de 05-08, e **não é para
consertar aqui**. Para regressão, use `npm run verificar-fase4` e `npm run verificar-fase2`.
</contrato_herdado>

<protocolo_de_disco>
Volume a 96% com histórico de despejo do iCloud: `stat` informa tamanho e a leitura devolve
zero byte. `buscar.tsx` tem 36.872 bytes e `feed.tsx` 6.485 em HEAD — confirme os dois com
`wc -c` contra `git show HEAD:<arquivo> | wc -c` **antes de editar**. Divergiu ou leu zero,
restaure com `git show HEAD:<arquivo> > <arquivo>`, nunca com `brctl download`.

Antes de cada commit leia `git diff --cached --numstat`; depois dele confira os bytes NO GIT
com `git show HEAD:<arquivo> | wc -c` e leia `git diff-tree --name-status -r HEAD`. Empurre
com `git push espelho HEAD`. Se rodar `gsd-tools query state.update-progress`, confira o
STATE.md em seguida.
</protocolo_de_disco>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Descobrir na visão web — grade de várias colunas e o destaque curado ocupando mais de uma</name>
  <files>src/app/(app)/descobrir/page.tsx, src/componentes/feed.tsx, src/estilos/web-descobrir.css</files>
  <precondition>`src/estilos/web-descobrir.css` existe vazia de regra, criada por 05-01, e `src/estilos/web.css` já traz `.web-grade`.</precondition>
  <behavior>
    - Na visão web, o contêiner do feed tem `data-grade-web` e mais de uma coluna medida: a
      largura de um cartão comum é menor que metade da largura do contêiner.
    - O cartão com `data-especial` também carrega `data-destaque-curado` e é MEDIDO mais largo
      que um cartão comum na mesma grade.
    - O selo de motivo de todos os cartões visíveis tem altura maior que zero na visão web,
      com o modo comentado desligado.
    - Na visão app, o feed continua em coluna única: a largura de um cartão é igual à largura
      útil do contêiner, dentro da tolerância de um pixel.
    - Trocar de persona e de disposição continua trocando o feed nas duas visões.
  </behavior>
  <action>
    A divergência é de LAYOUT e mora em CSS (D-79, D-05). É proibido criar `FeedWeb` como
    arquivo irmão e é proibido ramificar em JavaScript por visão. `feed.tsx` continua sendo o
    mesmo componente de cliente, com a mesma árvore de JSX e o mesmo cálculo de máscara de
    bits; o que muda é que o contêiner de cartões ganha `data-grade-web` e a classe
    `.web-grade` de `web.css`, e que o invólucro de um cartão especial ganha
    `data-destaque-curado`.

    O cartão especial é o que já carrega `data-especial` (destaque curado de D-29 e
    serendipidade de D-30). Não invente um segundo critério: leia o mesmo que `cartao.tsx` já
    usa e marque o invólucro. Em `web-descobrir.css`, sob `[data-view="web"]`, dê a ele
    `grid-column: span 2` — é isso, e não uma borda mais grossa, que faz o destaque ser
    visivelmente outra coisa numa grade.

    Escolha o número de colunas pela variável CSS que `.web-grade` expõe, definindo-a em
    `web-descobrir.css`. Três colunas dentro dos 1.152 px de `max-w-6xl` da moldura web dão
    cartão confortável; meça antes de fixar. **Não use media query** — a visão é estado da
    aplicação e não tamanho de janela (D-01, D-02); a única media query legítima do projeto é
    a da moldura, em `globals.css`.

    Em `descobrir/page.tsx`, o contêiner externo hoje é `desk:mx-auto desk:max-w-5xl
    desk:p-8`. Suba para a largura que a grade de três colunas pede e ponha o cabeçalho, o
    `<SeletorDisposicao>` e o `<TrocaPersona>` numa faixa superior que atravessa a largura
    toda, em vez de empilhados acima da grade em coluna estreita. Utilitário `desk:` resolve
    aqui, porque são classes do Tailwind.

    **O selo de motivo é o argumento da tela e na web ele fica sempre visível** (tela 25). Ele
    já é visível hoje; garanta que a grade não o corte nem o empurre para fora do cartão —
    `web.css` não pode ser editada, então qualquer ajuste de altura mínima do cartão em grade
    vai em `web-descobrir.css`.

    O bloco `<Diagnostico>` continua inteiro dentro de `<Comentario>`: ele é endereçado a quem
    avalia, e o modo comentado desligado tem de removê-lo sem furar a grade — `display: none`
    remove a caixa e o `gap` da grade não abre espaço entre irmãos ausentes.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-02.ts descobrir</automated>
  </verify>
  <done>Na visão web `/descobrir/` mostra grade de mais de uma coluna com o destaque curado medido mais largo que um cartão comum e todos os selos de motivo com altura maior que zero; na visão app o feed continua em coluna única; console sem erro nas duas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Buscar na visão web — facetas em coluna permanente e resultados em grade</name>
  <files>src/componentes/buscar.tsx, src/estilos/web-buscar.css</files>
  <precondition>`src/componentes/buscar.tsx` lê 36.872 bytes iguais a `git show HEAD:src/componentes/buscar.tsx | wc -c` antes da primeira edição.</precondition>
  <behavior>
    - Na visão web, o painel de facetas tem `data-coluna-facetas`, está visível ao carregar a
      tela SEM nenhum clique, e o seu retângulo fica à esquerda do retângulo dos resultados
      (borda direita das facetas menor ou igual à borda esquerda dos resultados).
    - As facetas continuam visíveis depois de rolar a página: a coluna é `sticky` e o seu topo
      permanece dentro da janela.
    - Marcar uma faceta recalcula o número de resultados sem navegar: a URL não muda.
    - Os resultados aparecem em grade de mais de uma coluna na visão web, com o tipo etiquetado
      visível em cada um.
    - Na visão app, o mesmo painel de facetas continua atrás do controle que a fase 3 desenhou
      e a lista continua em coluna única.
  </behavior>
  <action>
    Mesmo componente, mesma lógica de consulta (D-79). `consultar` e `facetasDe` de
    `indice.ts` não mudam, o DTO não muda, e o filtro continua sendo linear em memória a cada
    tecla. O que muda é onde o painel de facetas vive.

    Hoje, na visão app, as facetas ficam atrás de um controle — leia `buscar.tsx` e descubra
    como esse estado é guardado antes de mexer. Na visão web o painel tem de estar **aberto e
    ancorado**: envolva a área de facetas num elemento com `data-coluna-facetas` e a classe
    `.web-coluna-fixa` de `web.css`, e faça o par facetas + resultados usar
    `.web-duas-colunas`. Sob `[data-view="web"]`, em `web-buscar.css`, a regra força a coluna
    a ficar aberta independentemente do estado do controle — e o controle em si fica escondido
    na visão web, porque abrir e fechar uma coluna que é permanente é um botão que não faz
    nada. Não remova o controle da árvore: ele é o mesmo componente servindo as duas visões, e
    quem some é a caixa, por CSS.

    **A armadilha a evitar aqui é a que a fase 4 pagou duas vezes.** Um painel que existe no
    DOM e está fora da janela, ou coberto pela primeira linha de resultados, passa em gate de
    presença e está visivelmente quebrado. Escreva o CSS medindo: a coluna de facetas tem
    largura própria, os resultados ocupam o resto, e nenhum dos dois sobrepõe o outro. Confira
    com o gate da task, que compara retângulos.

    Os resultados vão para grade na visão web, com `.web-grade` e um número de colunas
    definido em `web-buscar.css`. Cada resultado mantém o tipo etiquetado que a fase 3 pôs
    (tela 28 pede exatamente isso) e continua usando `<CapaSemImagem>` e `<SelosDeLinguagem>`;
    **não passe a usar `<Cartao>`** — 03-04 registrou por escrito que um resultado de busca não
    chegou por aresta nenhuma e carimbar um motivo composto afirmaria relação inexistente.

    A tradução da busca em linguagem natural fica sempre visível na visão web (tela 28): o
    link para `/buscar/frase/` que já existe passa a mostrar, na web, a frase do Cenário 5
    junto do convite, em vez de só o convite. Leia `FRASE_DO_CENARIO_5` de `frase.ts` por
    `import type` mais valor — `frase.ts` não importa `grafo.ts`, então é seguro no cliente.

    Acrescente na coluna de facetas um link para `/filtros/`, com o texto explicando que ali as
    8 dimensões de acessibilidade são critério e não selo. **A rota `/filtros/` é criada por
    05-06 nesta mesma onda e não existe enquanto você trabalha.** O export estático não valida
    href interno e o build não falha; quem prova que o link resolve é o gate de 05-08.

    A faceta de gratuidade continua RECUSADA, com o motivo em texto de produto na tela, como
    03-04 a deixou: 0 dos 300 eventos declaram ingresso e o filtro passaria 100% dos eventos
    datados. Não a reintroduza por simetria com as outras.
  </action>
  <verify>
    <automated>npm run build && node_modules/.bin/tsx scripts/sonda-05-02.ts buscar</automated>
  </verify>
  <done>Na visão web `/buscar/` abre com a coluna de facetas aberta e à esquerda dos resultados, ela continua dentro da janela depois de rolar, marcar uma faceta recalcula sem navegar, e os resultados estão em grade com o tipo etiquetado; na visão app nada mudou.</done>
</task>

</tasks>

<sonda_do_plano>
As duas tasks pedem `scripts/sonda-05-02.ts`, sonda descartável deste plano — **apague-a antes
do último commit** e confirme com `git status --short scripts/` vazio. A suíte permanente é de
05-08.

A API real de `scripts/navegador.mjs`, que é esta e não a presumida: `servir({raiz})` de
`servir-out.mjs` devolve `{url, fechar}`; `abrirNavegador()` devolve o `cdp` diretamente; o
método é `cdp.navegar(url)`; o console acumulado está em `cdp.consola`; `cdp.clicar` recebe uma
EXPRESSÃO JavaScript que avalia para o elemento, não um seletor CSS; `cdp.avaliar(expressao)` e
`naPagina(corpo)` embrulham com o preludio. **Não existe domínio `Network.*`** — requisição
externa se prova com `performance.getEntriesByType('resource')` de dentro da página.
`visiveis()` do preludio falha sobre SVG.

Ponha a visão web por `localStorage.setItem("agenda-cultural:visao", "web")` seguido de
recarregar — a chave guarda **string crua**, não JSON — e espere `data-hidratado="sim"` antes
de medir. A moldura na visão web é `.moldura` com `max-w-6xl`; meça larguras contra o
retângulo dela, não contra `innerWidth`.

**Onde o gate afirmar que a tela é USÁVEL, meça geometria contra o contêiner** — retângulo do
elemento contra retângulo da moldura, `scrollWidth` contra `clientWidth` — e não presença nem
visibilidade. A fase 4 registrou dois gates verdes sobre tela quebrada, um por índice fora da
janela e outro por primeira linha coberta. Defina `DIR_CAPTURAS` e guarde uma foto de cada
tela nas duas visões, no diretório de rascunho e **fora do repositório**, para um humano
julgar o que o número não pega.
</sonda_do_plano>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → cliente | `buscar.tsx` é `"use client"` e não pode alcançar `@/dados/grafo` nem transitivamente (DP-F) |
| localStorage → tela | `agenda-cultural:persona`, `:disposicoes` e `:visao` são editáveis pelo avaliador |
| artefato → rede | zero requisição externa é promessa medida, não intenção |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information disclosure | import novo em `buscar.tsx` alcançando `grafo.ts` | high | mitigate | só `indice.ts` e `frase.ts`, que não importam `grafo`; gate DP-F transitivo herdado de 04-05 |
| T-05-06 | Denial of service | coluna de facetas cobrindo os resultados | high | mitigate | gate compara retângulos e exige facetas à esquerda sem sobreposição, mais captura de tela |
| T-05-07 | Spoofing | usar `<Cartao>` para resultado de busca, carimbando motivo inexistente | medium | mitigate | proibido no plano; segue `<CapaSemImagem>` + `<SelosDeLinguagem>` como 03-04 fixou |
| T-05-08 | Tampering | persona inválida em localStorage esvaziando o feed | low | accept | `feed.tsx` já cai na persona padrão (T-02-02), comportamento herdado e testado |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **este plano não instala pacote nenhum**; zero dependência nova |
</threat_model>

<verification>
1. `npm run build` compila e exporta.
2. `npm run verificar-fase2` e `npm run verificar-fase4` continuam verdes — Descobrir e Buscar
   são medidas por gates da fase 2 e da fase 3, e nenhum deles pode ser relaxado.
3. `npm run verificar-comentado` continua verde: o modo comentado desligado não pode furar a
   grade.
4. `verificar-fase3` vermelha só no gate 8 de `globals.css` — herdado de 05-01, não conserte.
5. Console sem erro nem aviso da aplicação nas quatro navegações (duas telas × duas visões).
6. Zero requisição externa.
7. `git diff --stat` não mostra `src/app/globals.css` nem `src/estilos/web.css`.
</verification>

<success_criteria>
- `/descobrir/` na visão web: grade de mais de uma coluna, destaque curado medido mais largo,
  selos de motivo todos com altura maior que zero.
- `/buscar/` na visão web: facetas abertas, à esquerda, ancoradas na rolagem, sem cobrir os
  resultados; resultados em grade com tipo etiquetado; tradução da busca em linguagem natural
  visível.
- Visão app inalterada nas duas telas, provada por medição e não por afirmação.
- Zero componente irmão criado; zero ramo em JavaScript decidindo layout por visão.
- Peso de `out/_next/static/chunks` cresce no máximo 20 KB.
- A sonda descartável foi apagada.
</success_criteria>

<output>
Create `.planning/phases/05-camada-2-web-desktop-e-profundidade/05-02-SUMMARY.md` when done
</output>
