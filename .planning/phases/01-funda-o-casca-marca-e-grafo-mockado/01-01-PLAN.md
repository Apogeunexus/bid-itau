---
phase: 01-funda-o-casca-marca-e-grafo-mockado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - next-env.d.ts
  - .gitignore
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/contexto/visao.tsx
  - src/componentes/casca.tsx
  - src/dados/tipos.ts
  - src/dados/grafo.ts
  - src/dados/gerado/entidades.json
  - src/dados/gerado/arestas.json
  - src/dados/gerado/vocabulario.json
  - src/dados/gerado/meta.json
  - src/app/verificacao/page.tsx
  - scripts/gerar-grafo.mjs
  - scripts/smoke-grafo.ts
autonomous: false
requirements: [FUND-01, FUND-02, DADO-01, DADO-05]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run build` termina sem erro e produz `out/index.html` — export estático, sem backend (FUND-01, D-24)."
    - "`npm run dev` serve `/` e `/verificacao`, e `/verificacao` exibe o título de uma entidade real vinda de `dados/`, não uma string escrita à mão (DADO-01)."
    - "Um botão fixo no canto alterna `data-view` entre `mobile` e `web`, e a escolha sobrevive a um reload (FUND-02, D-01, D-04)."
    - "`npx tsx scripts/smoke-grafo.ts` imprime um caminho de 2 passos ou mais entre duas entidades reais e sai com código 0."
    - "Toda entidade do grafo gerado carrega `procedencia`, e toda entidade `ic` carrega `fonte` não vazia (DADO-05, D-17)."
  artifacts:
    - "package.json com os scripts `dev`, `build`, `gerar-grafo` e `smoke`"
    - "next.config.ts com `output: \"export\"`"
    - "src/dados/tipos.ts — o contrato de tipos do grafo, completo, incluindo classes ainda não populadas"
    - "src/dados/grafo.ts — as seis funções de travessia de D-16 mais `slugsPorTipo`, `porSlug` e `contagens`"
    - "scripts/gerar-grafo.mjs — espinha do gerador, reexecutável e determinística"
    - "src/dados/gerado/{entidades,arestas,vocabulario,meta}.json versionados"
    - "src/contexto/visao.tsx — o ViewProvider que escreve `data-view`"
  key_links:
    - "`gerar-grafo.mjs` → `src/dados/gerado/*.json` → `grafo.ts` → `/verificacao`: se qualquer elo quebrar, a página de verificação não renderiza e o build falha."
    - "`ViewProvider` → atributo `data-view` no elemento raiz → variantes `app:`/`desk:` do Tailwind: sem o atributo, nenhuma classe de visão resolve."
    - "`slugsPorTipo` → `generateStaticParams` das rotas dinâmicas: sob `output: export` uma rota dinâmica sem essa lista não é exportada."
---

<objective>
Traçador vertical da fase: fazer o protótipo existir do disco à tela, atravessando **todas** as
camadas que a fase vai construir, por um único caminho fino.

Ao fim deste plano o projeto Next.js roda, faz export estático, tem o alternador de visões
funcionando, e `/verificacao` mostra o título de uma entidade real do acervo do Itaú Cultural
que saiu de `dados/` e passou pelo gerador, pelos tipos e pelas funções de travessia.

Não é protótipo descartável: os arquivos criados aqui são os mesmos que os planos `01-02` e
`01-03` vão **expandir**. O contrato de `src/dados/tipos.ts` e a assinatura de
`src/dados/grafo.ts` são definidos completos aqui, mesmo que a geração ainda popule só parte
das classes — é isso que permite os dois planos seguintes rodarem em paralelo sem se
esperarem.

Purpose: provar a arquitetura inteira em um commit, antes de dez camadas estarem escritas.
Output: projeto Next.js 16 + TypeScript + Tailwind 4 que builda estático, lê o grafo e alterna
as duas visões.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-casca-marca-e-grafo-mockado/1-CONTEXT.md
@.planning/intel/constraints.md
</context>

<fatos_medidos>
Números verificados diretamente nos arquivos em disco durante o planejamento. Use-os como
expectativa; se o gerador produzir número diferente, é bug do gerador, não do plano.

| Fonte | Registros | Observação |
|---|---|---|
| `dados/normalizado/agentes.json` | 152 | campo `papeis` só contém `colunista` (150) e `participante` (2) |
| `dados/normalizado/conteudos.json` | 1.805 | 1.064 com linguagem · 1.162 com tema · só 63 com agente |
| `dados/normalizado/eventos.json` | 100 | todos com imagem · 89 com período · 0 com espaço, território ou preço |
| `dados/normalizado/formacoes.json` | 54 | |
| `dados/normalizado/midias.json` | 529 | 336 podcasts · 63 séries · 46 vídeos |
| `dados/normalizado/publicacoes.json` | 46 | |
| **Total normalizado** | **2.686** | o número declarado de 2.534 exclui `agentes` |
| `dados/amostra/enciclopedia.jsonl` | 1.766 | 481 termo · 423 pessoa · 246 instituicao · 239 obra · 217 grupo · 160 evento |
| `dados/imagens/` | 900 + `indice.json` | 135 MB · `indice.json` mapeia `{arquivo: {url, dono}}` |

Ambos os conjuntos usam identificadores próprios; `dados/normalizado/*` já traz `id` no
formato `"{tipo}:{n}"` (ex.: `"evento:13913"`), e `indice.json` usa `dono` no formato
`"enc:pessoa:26400"` ou `"midia:1661"`.
</fatos_medidos>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: conferir legitimidade dos pacotes npm antes de instalar</name>
  <what-built>
Ainda não foi construído nada — este é o gate que precede o primeiro `npm install`.

Não existe RESEARCH.md com auditoria de pacotes nesta fase (a pesquisa foi dispensada por
decisão do usuário). Pela política de fallback, todo pacote entra como `[ASSUMED]` e exige
confirmação humana antes da instalação. A lista completa a instalar:

Runtime: `next@16` · `react@19` · `react-dom@19`
Dev: `typescript@5` · `@types/node` · `@types/react` · `@types/react-dom` ·
`tailwindcss@4` · `@tailwindcss/postcss@4` · `tsx@4` · `clsx@2`
  </what-built>
  <how-to-verify>
Leva 30 segundos. Abra, se quiser conferir na fonte:
`https://npmjs.com/package/next` · `https://npmjs.com/package/tailwindcss` ·
`https://npmjs.com/package/@tailwindcss/postcss` · `https://npmjs.com/package/tsx` ·
`https://npmjs.com/package/clsx`

Três coisas a olhar:
1. O nome está escrito exatamente como acima — sem letra trocada, sem sufixo estranho.
2. O publisher é o esperado: `vercel`, `tailwindlabs`, `privatenumber`, `lukeed`.
3. Os downloads semanais estão na casa dos milhões, não das dezenas.

Nenhum destes pacotes é obscuro; o gate existe para pegar typosquat, não para debater a
escolha da stack, que já está travada por FUND-01.
  </how-to-verify>
  <resume-signal>
Responda "ok" para liberar a instalação. Se algum nome, publisher ou volume não bater,
descreva o que viu — nesse caso nada é instalado e o plano para aqui.
  </resume-signal>
</task>

<task type="tracer">
  <name>Task 1: Fatia vertical — do JSON em disco à tela, nas duas visões, com export estático</name>
  <precondition>`dados/normalizado/eventos.json` e `dados/amostra/enciclopedia.jsonl` existem e são legíveis a partir da raiz do projeto.</precondition>
  <files>package.json, next.config.ts, tsconfig.json, postcss.config.mjs, .gitignore, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/contexto/visao.tsx, src/componentes/casca.tsx, scripts/gerar-grafo.mjs, src/dados/tipos.ts, src/dados/grafo.ts, src/app/verificacao/page.tsx</files>
  <action>
Um único caminho atravessando todas as camadas da fase. Nada de segunda rota, nada de segundo
tipo de entidade, nada de lote. Tratamento de erro real neste caminho — nos outros, nem existe
caminho ainda.

**Camada 0 — projeto (FUND-01, D-24).**
Rodar `npm install` com os 11 pacotes aprovados no checkpoint. Não usar `create-next-app`:
escrever os arquivos de configuração à mão, para que nada de scaffold genérico entre no repo.
- `package.json`: `"type": "module"`, scripts `dev` (`next dev`), `build` (`next build`),
  `start` (`next start`), `gerar-grafo` (`node scripts/gerar-grafo.mjs`) e
  `smoke` (`tsx scripts/smoke-grafo.ts`).
- `next.config.ts`: `output: "export"`, `images: { unoptimized: true }`,
  `trailingSlash: true`. Nada de rewrites, headers ou redirects — não existem em export estático.
- `tsconfig.json`: `strict: true`, `resolveJsonModule: true`, `moduleResolution: "bundler"`,
  path alias `"@/*": ["./src/*"]`, `jsx: "preserve"`, plugin `next`.
- `postcss.config.mjs`: apenas `{ plugins: { "@tailwindcss/postcss": {} } }`.
- `.gitignore`: acrescentar `/.next/`, `/out/`, `/node_modules/`, `next-env.d.ts`, e o par
  `/public/acervo/*` seguido de `!/public/acervo/indice.json` — as 900 imagens somam 135 MB e
  são reproduzíveis pelo gerador; o índice de procedência, não. As linhas já presentes ficam.

**Camada 1 — gerador (DADO-01, D-15).**
`scripts/gerar-grafo.mjs`, Node puro, sem dependência, executável por `npm run gerar-grafo`,
idempotente e determinístico. Nesta task ele faz só a espinha:
- Lê `dados/normalizado/eventos.json` e `dados/amostra/enciclopedia.jsonl`.
- Emite `src/dados/gerado/entidades.json` com **duas** classes: `evento` (os 100 do CMS) e
  `pessoa` (as 423 da Enciclopédia). As demais classes ficam para o plano `01-03`.
- Emite `src/dados/gerado/vocabulario.json` com as 29 linguagens de
  `dados/taxonomia/linguagens.json`, cada uma com `id`, `rotulo` e `cor` — e `cor` guarda o
  **nome do token CSS** (`"--ic-lilas"`, `"--ic-azul"`, ...), nunca o hex. O hex tem uma única
  fonte de verdade, que é `globals.css`. Isso é D-08: a cor da linguagem é dado, não estilo.
- Emite `src/dados/gerado/arestas.json` (pode sair vazio nesta task) e
  `src/dados/gerado/meta.json` com `geradoEm`, contagem por classe e contagem por procedência.
- Envelope obrigatório em toda entidade, sem exceção (D-17, DADO-05): `procedencia` em
  `"ic" | "derivado" | "autorado"` e, quando `"ic"`, `fonte` com a URL de origem. Para o CMS,
  `fonte` = `https://www.itaucultural.org.br/{categoria}/{slug}`; para a Enciclopédia, `fonte` =
  o campo `url` do registro, que já vem pronto.
- `id` no formato `"{classe}:{origem}:{idOrigem}"`, com `origem` em `cms | enc | derivado |
  autorado` — ex.: `evento:cms:13913`, `pessoa:enc:26400`. Esse formato é contrato: os planos
  seguintes e as fases 2 a 6 dependem dele.
- `resumo` sai como **texto puro**. Os campos do CMS carregam HTML colado do Word com resíduos
  `mso-` e entidades escapadas; remover tags, decodificar entidades e colapsar espaços dentro
  do gerador. Nenhum HTML cru atravessa a fronteira do gerado — ver o registro T-01-02 do
  modelo de ameaças.
- Determinismo: ordenar toda saída por `id` antes de escrever, e serializar com
  `JSON.stringify(x, null, 2)`. Rodar duas vezes seguidas tem de produzir bytes idênticos.

**Camada 2 — contrato de tipos (`src/dados/tipos.ts`).**
Escrever o contrato **completo** da ontologia agora, mesmo com classes que só serão populadas
no plano `01-03`. É este arquivo que permite os dois planos de onda 2 correrem em paralelo.
- `Procedencia = "ic" | "derivado" | "autorado"` (D-17).
- `ClasseEntidade` cobrindo as seis camadas da ontologia: `pessoa`, `coletivo`, `instituicao`,
  `espaco`, `obra`, `termo`, `programa`, `evento`, `temporada`, `ocorrencia`, `conteudo`,
  `midia`, `publicacao`, `formacao`, `linguagem`, `tema`, `territorio`, `pessoa-usuaria`,
  `repertorio`, `trilha`.
- `Relacao` com o vocabulário de PRD §6: `influenciou`, `dialoga_com`, `deriva_de`,
  `pertence_a`, `atua_em`, `curou`, `realiza`, `ocorre_em`, `situado_em`, `aprofunda`,
  `fala_sobre`, `contextualiza`, `semelhante_a`, `duplicata_suspeita`.
- `Acessibilidade` com as 8 dimensões do CMS (`audio_description`, `libras`,
  `descriptive_subtitle`, `closed_caption`, `open_caption`, `simultaneous_translation`,
  `stenotypy`, `subtitle`), todas `boolean`.
- `Entidade` com `id`, `classe`, `titulo`, `slug`, `resumo?`, `imagem?`, `creditoImagem?`,
  `linguagens: string[]`, `temas: string[]`, `acessibilidade`, `procedencia`, `fonte?`,
  `chaveIdentidade?`, e um `extra?: Record<string, unknown>` para o que é específico de classe.
- `Aresta` com `de`, `para`, `relacao`, `procedencia`, `peso?`, e dois campos condicionais:
  `papel?` (obrigatório quando a relação é `atua_em` — DADO-03, o papel mora na aresta e nunca
  vira classe) e `motivo?` (obrigatório quando a relação é `semelhante_a` — DADO-04, D-18).
- `Passo = { de: Entidade; para: Entidade; relacao: Relacao; motivo?: string; papel?: string }`.
- `Temporada` e `Ocorrencia` como interfaces próprias, separadas de `Entidade` no campo `extra`
  (DADO-02): `Temporada` tem `eventoId`, `espacoId?`, `inicio`, `fim`; `Ocorrencia` tem
  `temporadaId`, `eventoId`, `inicio` (datetime ISO), `espacoId?`, `preco?`, `gratuito`,
  `esgotado`, `acessibilidade`.

**Camada 3 — travessia mínima (`src/dados/grafo.ts`).**
Nesta task, só `porId` e `vizinhos`, ambos sobre índices `Map` construídos uma vez no
carregamento do módulo. Importa os JSON gerados diretamente. As outras funções de D-16 entram
na Task 2 — mas a assinatura completa já é decidida lá, não aqui.

**Camada 4 — casca e alternador (FUND-02, D-01 a D-05).**
- `src/contexto/visao.tsx`: `ViewProvider` como client component, estado
  `"mobile" | "web"`, valor inicial `"mobile"`, leitura e escrita em `localStorage` sob a chave
  `agenda-cultural:visao`. Ler o `localStorage` dentro de `useEffect` e não na primeira
  renderização — em export estático o HTML é gerado no build e ler storage na renderização
  produz divergência de hidratação. Expõe `useVisao()`.
- `src/componentes/casca.tsx`: client component que recebe `children`, escreve
  `data-view={visao}` no seu elemento raiz, e renderiza dois filhos — o botão alternador e o
  conteúdo. O alternador é um par de botões fixo no canto (`fixed`), fora do fluxo, visível nas
  duas visões (D-04). Quando `data-view="mobile"`, o conteúdo vai dentro de uma moldura de
  celular centralizada de 390 px com cantos arredondados (D-03); em viewport estreito a moldura
  some — e **esta** é a única exceção legítima a media query no projeto, porque é sobre o
  tamanho real da janela e não sobre a visão escolhida.
- `src/app/layout.tsx`: server component, `lang="pt-BR"`, importa `globals.css`, envolve tudo
  em `ViewProvider` e `Casca`.
- `src/app/globals.css`: `@import "tailwindcss";` mais o mínimo desta task — as três cores
  principais como custom properties em `:root` (`--ic-laranja: #ff7800`, `--ic-preto: #000000`,
  `--ic-branco: #ffffff`), um bloco `@theme` expondo-as ao Tailwind (D-06), e as duas variantes
  customizadas (D-02):
  `@custom-variant app (&:is([data-view="mobile"] *, [data-view="mobile"]));` e
  `@custom-variant desk (&:is([data-view="web"] *, [data-view="web"]));`
  O `:is(... *, ...)` cobre tanto os descendentes quanto o próprio elemento que carrega o
  atributo. A paleta de apoio e a tipografia entram no plano `01-02`.
  Regra dura, herdada de D-02: a divergência entre as duas visões mora nas variantes `app:` e
  `desk:`. As variantes de breakpoint nativas do Tailwind estão proibidas neste projeto, porque
  amarrariam o alternador ao tamanho da janela do avaliador. Não repita esta proibição como
  comentário dentro de `src/` — ela é verificada por varredura no código.

**Camada 5 — a tela (`src/app/page.tsx` e `src/app/verificacao/page.tsx`).**
- `/`: redireciona para `/descobrir` via `redirect()` do `next/navigation`. Como `/descobrir`
  ainda não existe nesta task, criar `/` como uma página simples com o nome do produto e um
  link para `/verificacao`; o plano `01-02` a converte em redirect quando as abas existirem.
- `/verificacao`: server component. Chama `porId` com o id de um evento real do CMS e o id de
  uma pessoa real da Enciclopédia, e renderiza título, classe, procedência e fonte de cada um.
  Se `porId` devolver `undefined`, lançar erro com mensagem explícita — em `output: export` a
  página é renderizada no build, então um grafo quebrado derruba o build em vez de virar tela
  branca em apresentação. Aplicar `bg-[var(--ic-branco)]`, `text-[var(--ic-preto)]` e um
  acento em `--ic-laranja`, e usar ao menos uma classe `app:` e uma `desk:` para provar as
  variantes.
  </action>
  <verify>
    <automated>npm run gerar-grafo &amp;&amp; npm run build &amp;&amp; test -f out/index.html &amp;&amp; test -f out/verificacao/index.html &amp;&amp; node -e "const e=require('./src/dados/gerado/entidades.json');const l=Array.isArray(e)?e:e.entidades;if(!l.length)throw new Error('grafo vazio');const semProc=l.filter(x=>!x.procedencia);if(semProc.length)throw new Error(semProc.length+' entidades sem procedencia');const icSemFonte=l.filter(x=>x.procedencia==='ic'&amp;&amp;!x.fonte);if(icSemFonte.length)throw new Error(icSemFonte.length+' entidades ic sem fonte');console.log('entidades',l.length)"</automated>
    <automated>npm run gerar-grafo &amp;&amp; cp src/dados/gerado/entidades.json /tmp/g1.json &amp;&amp; npm run gerar-grafo &amp;&amp; diff -q /tmp/g1.json src/dados/gerado/entidades.json</automated>
    <automated>grep -q 'data-view' src/componentes/casca.tsx &amp;&amp; grep -q 'custom-variant app' src/app/globals.css &amp;&amp; grep -q 'custom-variant desk' src/app/globals.css &amp;&amp; grep -q 'output: *"export"' next.config.ts</automated>
    <automated>test $(grep -rnE "\b(sm|md|lg|xl|2xl):" src/ --include='*.tsx' --include='*.ts' | grep -v '^\s*//' | wc -l) -eq 0</automated>
    <automated>test $(grep -rn "dangerouslySetInnerHTML" src/ scripts/ | wc -l) -eq 0</automated>
  </verify>
  <done>
`npm run build` produz `out/index.html` e `out/verificacao/index.html`. A página de verificação
exibe o título de um evento real e de uma pessoa real vindos do acervo em disco. O gerador é
determinístico entre execuções. O elemento raiz da casca carrega `data-view` e as duas variantes
Tailwind existem. Nenhuma classe de breakpoint nativa e nenhuma injeção de HTML cru no código.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fechar o contrato de travessia de D-16 e provar o caminho multi-salto</name>
  <files>src/dados/grafo.ts, scripts/gerar-grafo.mjs, scripts/smoke-grafo.ts, package.json</files>
  <action>
A fatia do traçador provou o encanamento. Esta task fecha a **API pública do grafo**, que é o
contrato do qual os planos `01-02` e `01-03` dependem em paralelo e que as fases 2 a 6 vão
consumir. Depois desta task, `01-03` pode reescrever tudo debaixo dela sem mexer em assinatura.

**Arestas mínimas no gerador.** Estender `scripts/gerar-grafo.mjs` para emitir, além do que já
emite:
- Nós de `linguagem` como entidades de primeira classe (classe `linguagem`, procedência `ic`,
  fonte apontando para a taxonomia), um por entrada do vocabulário controlado.
- Aresta `pertence_a` de cada entidade para cada linguagem que ela declara. É o que conecta o
  grafo — sem isso não há caminho entre nada.
- Aresta `semelhante_a` entre pares de entidades da mesma classe que compartilham linguagem,
  com `motivo` em português legível montado a partir do que de fato é compartilhado, no formato
  "parecido porque os dois são {classe} de {linguagem}". Nunca emitir `semelhante_a` sem
  `motivo` — aresta sem motivo é erro de geração e o gerador deve abortar, não completar depois
  (D-18, DADO-04). Limitar a fanout fixo por entidade (as 20 mais próximas por número de
  atributos compartilhados, desempate por `id`) para o arquivo não explodir — a expansão para
  território e tema é do plano `01-03`.

**As seis funções de D-16, mais três de serviço**, todas em `src/dados/grafo.ts`, todas
puras e síncronas, todas sobre índices `Map` construídos uma vez:
- `porId(id: string): Entidade | undefined`
- `vizinhos(id: string, relacao?: Relacao): Array<{ aresta: Aresta; entidade: Entidade }>`
- `caminho(de: string, para: string, maxPassos = 4): Passo[] | null`
- `porLinguagem(linguagemId: string, classe?: ClasseEntidade): Entidade[]`
- `porTerritorio(territorioId: string, janela?: { de: string; ate: string }): Entidade[]`
- `ocorrenciasDe(eventoId: string): Ocorrencia[]`
- `slugsPorTipo(classe: ClasseEntidade): string[]` — fora de D-16, exigida porque sob
  `output: "export"` (D-24) toda rota dinâmica precisa alimentar `generateStaticParams`.
- `porSlug(classe: ClasseEntidade, slug: string): Entidade | undefined`
- `contagens(): { porClasse: Record<string, number>; porProcedencia: Record<string, number> }`

`porTerritorio` e `ocorrenciasDe` devolvem lista vazia enquanto as classes `territorio`,
`temporada` e `ocorrencia` não estiverem populadas — a assinatura existe e é honesta, o plano
`01-03` enche por baixo. Não colocar `throw new Error("não implementado")`: isso quebraria o
build do plano `01-02`, que roda em paralelo.

**`caminho()` precisa devolver um caminho que signifique alguma coisa.** Busca em largura sobre
as arestas, com duas travas que são a diferença entre uma trilha explicável e um truque:
1. Ordem de preferência de aresta na expansão: `semelhante_a` primeiro, depois as relações de
   acontecimento e agência (`atua_em`, `realiza`, `ocorre_em`, `situado_em`, `curou`), e só por
   último `pertence_a`. Sem isso todo par de entidades vira "dois saltos passando por Artes
   visuais", que é verdade e é inútil.
2. Trava de concentrador: um nó cujo grau passe de `GRAU_HUB = 60` não pode ser salto
   intermediário na primeira passada. Se a primeira passada não achar caminho, repetir sem a
   trava. Exportar `GRAU_HUB` para o valor ser auditável.
`caminho` devolve `Passo[]` com as entidades resolvidas e o `motivo`/`papel` de cada aresta —
é exatamente o que a tela "Por que isto apareceu" (fase 2) vai renderizar, e é o que torna a
recomendação explicável.

**`scripts/smoke-grafo.ts`**, rodado por `npm run smoke` (`tsx scripts/smoke-grafo.ts`). Node
nativo não importa JSON de dentro de `.ts` sem atributo de import — verificado durante o
planejamento, falha com `ERR_IMPORT_ATTRIBUTE_MISSING`; por isso `tsx`, e não `node` puro.
O script deve:
- imprimir `contagens()`;
- escolher dois ids reais do grafo **derivando-os do próprio conteúdo** (por exemplo a primeira
  `pessoa` e o primeiro `evento` por ordem de id) e não por constante escrita à mão, para não
  quebrar quando o plano `01-03` reescrever a geração;
- chamar `caminho(a, b)` e imprimir cada passo como
  `{titulo de} --{relacao}--> {titulo para}   ({motivo})`;
- sair com código 1 e mensagem explícita se o caminho for `null` ou tiver menos de 2 passos, e
  com 0 caso contrário.
  </action>
  <verify>
    <automated>npm run gerar-grafo &amp;&amp; npm run smoke</automated>
    <automated>npm run smoke | grep -cE '\-\-.*\-\->' | awk '{if($1&lt;2){print "caminho com menos de 2 passos";exit 1}}'</automated>
    <automated>node -e "const a=require('./src/dados/gerado/arestas.json');const l=Array.isArray(a)?a:a.arestas;const s=l.filter(x=>x.relacao==='semelhante_a');if(!s.length)throw new Error('nenhuma aresta semelhante_a');const semMotivo=s.filter(x=>!x.motivo||!x.motivo.trim());if(semMotivo.length)throw new Error(semMotivo.length+' arestas semelhante_a sem motivo');const at=l.filter(x=>x.relacao==='atua_em'&amp;&amp;!x.papel);if(at.length)throw new Error(at.length+' arestas atua_em sem papel');console.log('arestas',l.length,'semelhante_a',s.length)"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/dados/grafo.ts','utf8');for(const f of ['porId','vizinhos','caminho','porLinguagem','porTerritorio','ocorrenciasDe','slugsPorTipo','porSlug','contagens']){if(!new RegExp('export (async )?function '+f+'\\\\b').test(s))throw new Error('falta exportar '+f)}console.log('API de D-16 completa')"</automated>
    <automated>npm run build &amp;&amp; test -f out/verificacao/index.html</automated>
  </verify>
  <done>
`npm run smoke` imprime um caminho de 2 ou mais saltos entre duas entidades reais do acervo, com
o motivo de cada aresta, e sai com código 0. As nove funções estão exportadas de
`src/dados/grafo.ts`. Nenhuma aresta `semelhante_a` sem motivo e nenhuma `atua_em` sem papel.
O build estático continua passando.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco → gerador | `dados/normalizado/*.json` e `enciclopedia.jsonl` carregam HTML colado do Word e strings vindas de crawl; o gerador é o único ponto onde isso é saneado |
| gerador → `public/` | o passo de cópia de imagens escreve dentro do diretório servido, usando chaves vindas de `indice.json` |
| registro npm → projeto | 11 pacotes instalados sem auditoria de pesquisa prévia |
| navegador → `localStorage` | a persona (D-25) e a visão (D-04) são estado de cliente, sem servidor que valide |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `scripts/gerar-grafo.mjs` — escrita em `public/acervo/` | high | mitigate | validar toda chave de `indice.json` contra `^[0-9a-f]{8,}\.(jpg\|jpeg\|png\|webp)$` antes de usar como nome de arquivo destino, e resolver o caminho final com `path.resolve` conferindo que continua dentro de `public/acervo/`; chave fora do padrão é ignorada com aviso (implementado no plano `01-03`, contrato fixado aqui) |
| T-01-02 | Tampering | `resumo`/`shortDescription` do CMS renderizados na tela | high | mitigate | o gerador converte HTML em texto puro; nenhum componente usa injeção de HTML cru — verificado por varredura em `src/` e `scripts/` no `<verify>` da Task 1 |
| T-01-03 | Denial of Service | `caminho()` sobre nós concentradores | medium | mitigate | `maxPassos` com teto padrão 4 e trava `GRAU_HUB = 60` na primeira passada; busca em largura sobre índices `Map`, sem recursão irrestrita |
| T-01-04 | Spoofing | seleção de persona em `localStorage` (D-25) | low | accept | é mock explícito e rotulado como tal na tela; não existe backend, dado privado nem sessão a proteger no protótipo |
| T-01-05 | Information Disclosure | `fonte` com URL interna no artefato exportado | low | accept | as URLs são públicas (`itaucultural.org.br`, `enciclopedia.itaucultural.org.br`) e a exibição da procedência é requisito do produto (DADO-05, princípio 9) |
| T-01-SC | Tampering | instalação npm | high | mitigate | gate de legitimidade de pacotes como checkpoint humano bloqueante antes do primeiro `npm install`; `package-lock.json` versionado para congelar as resoluções |
</threat_model>

<verification>
1. `npm run gerar-grafo && npm run build` termina em código 0 e cria `out/index.html`.
2. `npm run smoke` imprime um caminho de 2 ou mais saltos e sai em 0.
3. `npm run dev` serve `/` e `/verificacao`; a página de verificação mostra dados reais.
4. Clicar no alternador troca `data-view` no DOM; recarregar a página preserva a escolha.
5. Rodar o gerador duas vezes produz bytes idênticos.
</verification>

<success_criteria>
- O projeto Next.js 16 + TypeScript + Tailwind 4 existe, roda e faz export estático (FUND-01).
- O alternador de visões escreve `data-view` e persiste em `localStorage` (FUND-02).
- O grafo é gerado por transformação de dados reais em disco, não escrito à mão (DADO-01).
- Toda entidade carrega procedência e toda entidade `ic` carrega fonte (DADO-05).
- A API de travessia de D-16 está exportada e `caminho()` devolve um caminho real.
</success_criteria>

<output>
Criar `.planning/phases/01-funda-o-casca-marca-e-grafo-mockado/01-01-SUMMARY.md` ao terminar.
Registrar no SUMMARY, obrigatoriamente: o formato final de `id`, a assinatura exata das nove
funções de `grafo.ts` e o valor de `GRAU_HUB` — os planos `01-02` e `01-03` leem isso.
</output>
