# S5 · Editor / Curador

sessão: bid-ita-7d [b1187f]
estado: rodando
tarefa: 1 de 10
pasta: `(bastidor)/redacao/`
prd: [`sessoes/S5-EDITOR.md`](../sessoes/S5-EDITOR.md)

> **Só a S5 escreve neste arquivo.** A sessão de controle lê e agrega no `PAINEL.md`.
> Uma entrega só entra na tabela **com hash de commit**. Sem hash, não aconteceu.

## Entregas

| # | tarefa | commit | quando | nota |
|---|---|---|---|---|
| 1 | conferir o split da S3 e reancorar o que sobrou | d1a0f79 | 25.08 21:31 | `redacao.ts` já saiu re-ancorado do split — os reexports estão lá e nenhuma das 44 classes `.redacao-*` ficou órfã. O desalinhado era a folha: 5 comentários descreviam a fila que mudou para `moderacao.css`, e um bloco inteiro estava duplicado no corte. Nenhuma regra de CSS mudou |

**A nota é obrigatória**: uma linha sobre o que foi feito, o que ficou de fora, ou o que
surpreendeu. O hash diz que aconteceu; a nota diz o quê.

## Bloqueios

**A E1 está parada esperando decisão do dono do projeto** (§9 do protocolo). Duas
bifurcações que eu não fecho sozinho, com o plano já escrito:

1. **Reordenar passo quebra a cadeia.** Uma trilha é `A→B→C`; mover o passo do meio faz
   `de`/`para` deixarem de bater com aresta nenhuma do acervo, e o motivo que explicava
   *aquela* ligação passa a explicar outra. Recomendo: reordena tudo, o texto **fica** no
   campo, e o passo é marcado «este motivo foi escrito para outra ligação» e conta como
   pendência até o curador confirmar ou reescrever.
2. **«Recálculo da publicabilidade ao vivo» colide com «não reescreva as quatro regras».**
   Três delas moram em `trilhaEhPublicavel` (`trilha.ts:437`) e rodam NO SERVIDOR, sobre a
   trilha do acervo; o cliente não pode chamá-las porque `redacao.ts` alcança 23 MB de grafo
   (DP-F). Hoje `publicavelNoAcervo` é veredito congelado do build e reordenar não o move.
   Recomendo: a página manda no DTO os dois fatos que as regras leem (`é evento`, `tem
   sessão datada`) por nó, e as mensagens **verbatim** de `trilhaEhPublicavel` — o cliente
   aplica os mesmos testes sobre fatos do servidor, sem inventar regra nem reescrever texto.
   É uma segunda avaliação da mesma regra, e por isso eu pergunto em vez de decidir.

## Pedidos de contrato

1. **PEDIDO · `/redacao/*` não tem entrada de menu nenhuma.** `menu-lateral.tsx:91` lista só
   «Moderação». As nove telas desta sessão ficam alcançáveis apenas por URL digitada, o que
   é um beco de navegação para o avaliador. O arquivo não é meu. **Enquanto não for
   atendido, faço navegação interna da superfície no meu próprio cabeçalho** — não fico
   parada por isso.
2. **PEDIDO · `globals.css:69`** ainda descreve `redacao.css` como «fila e editor de
   trilha». Ficou velho no split da S3. É o arquivo de colisão e eu não toco.
3. **Referência morta no PRD, sem impacto:** a seção de leitura manda ler `redacao.ts` «da
   linha 795 em diante», e o arquivo tem 421 linhas depois do split. Não precisa de ação.
