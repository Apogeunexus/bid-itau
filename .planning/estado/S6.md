# S6 · Organização

sessão: bid-ita-53 [913196]
estado: rodando
tarefa: 1 de 11
pasta: `(bastidor)/studio/` — **em paralelo com a S7**, sob a fronteira de 25.08 22:4x
prd: [`sessoes/S6-ORGANIZACAO.md`](../sessoes/S6-ORGANIZACAO.md)

> **Só a S6 escreve neste arquivo.** A sessão de controle lê e agrega no `PAINEL.md`.
> Uma entrega só entra na tabela **com hash de commit**. Sem hash, não aconteceu.

## Entregas

| # | tarefa | commit | quando | nota |
|---|---|---|---|---|
| 1 | ler o que a S7 deixou · folha própria · linha no arquivo de colisão | 4a14e13 | 25.08 22:57 | Li `tipos-acesso.ts` (780 linhas), `mock/seed.ts` e `studio-estado.ts` antes de escrever: **`Situacao`, as 8 dimensões, `FRASE_DO_ATO_DE_DECLARAR`, as três `PORTAS` e `catalogoDoStudio()` são reuso, não reescrita.** A folha traz só as **quatro** coisas que nenhum vocabulário anterior desenha — conversão de procedência à vista, ato de declarar ausência, coluna do que falta, atributos físicos do lugar; superfície, painel, pastilha e duas colunas vêm de `studio.css` e `web.css` por consumo de classe. Surpresa: a fronteira mudou a folha do PRD (`studio.css` → `organizacao.css`) e a §5 do PRD (visão app) caiu por D-67. |

**A nota é obrigatória**: uma linha sobre o que foi feito, o que ficou de fora, ou o que
surpreendeu. O hash diz que aconteceu; a nota diz o quê.

### O que a tarefa 1 mediu

```
verificar-ds               40 verdes · 1 falha (cursos.css, herdada de 4da75f5)
tsc --noEmit               VERMELHO em redacao-trilha.tsx e redacao.ts — arquivos da S5
                           em edição neste momento; esta entrega não tem 1 linha de TS
organizacao.css            231 linhas · 0 hex · 0 declaração de token do DS
classes novas              12, em 4 blocos — .org-conversao · .org-ato · .org-falta · .org-fisico
classes REUSADAS           .studio · .studio-cabecalho · .studio-painel · .studio-pastilha ·
                           .studio-rotulo · .studio-nota · .studio-literal ·
                           .web-duas-colunas · .web-colada · .web-painel · .web-declaracao ·
                           .web-denominador
globals.css                1 bloco, 15 linhas, entre admin.css e base.css
```

### O que eu herdo da S7 e não redeclaro

| Arquivo | O que eu consumo |
|---|---|
| `src/dados/tipos-acesso.ts` | `Situacao` e os 5 estados, `DIMENSOES_DE_ACESSIBILIDADE`, `acessibilidadeVazia()`, `algumaDimensaoMarcada()`, `FRASE_DO_ATO_DE_DECLARAR`, `PORTAS`, `ProcedenciaDePapel`, `CHAVE_DE_ARMAZENAMENTO` |
| `src/dados/mock/seed.ts` | `catalogoDoStudio()` — 113 espaços com `cidade`, `estado` e `declaraAcessibilidade`; `ORGANIZACAO_DO_PRODUTOR`; `DATA_DE_REFERENCIA` |
| `src/componentes/ficha-acessibilidade.tsx` | `FichaDeAcessibilidade` — a ficha das 8 em **leitura**, com os três estados de D-43 |

**O que eu NÃO herdo, e por quê.** `useStudio()` está preso a `RascunhoDoProdutor` e à chave
`studio.v1`. A Organização escreve outras entidades — espaço, instituição, mídia, colaborador,
edital, lote —, e reusar a mesma chave faria as duas sessões gravarem uma por cima da outra no
mesmo `localStorage`. A minha loja nasce em `studio-org-estado.ts` com chave própria, no
**mesmo padrão** de `useSyncExternalStore` + hidratação em `useEffect`. Isso é o padrão reusado;
não é uma segunda implementação da mesma coisa.

## Bloqueios

—

## Pedidos de contrato

### PEDIDO-S6-01 · `AcessibilidadeDeEspaco` — a lacuna mais importante
Rampa, elevador, banheiro adaptado, piso tátil e vaga reservada **não cabem** nas 8 dimensões
de `Acessibilidade`, que são de mídia. Forçar rampa dentro de `closed_caption` seria fabricar
classificação. Enquanto o pedido não é atendido, a estrutura entra por **extensão aditiva** no
meu módulo — no padrão do PEDIDO-01 da S7 —, com grade visual própria (`.org-fisico`), separada
da ficha das oito de propósito: duas grades iguais fariam quem confere achar que é uma ficha só
com treze linhas.

### PEDIDO-S6-02 · `edital` não existe em `ClasseEntidade`
As funcionalidades 46, 48 e 49 pressupõem edital no grafo. **Registrado antes da O6**, como o
PRD manda, e **não forçado** dentro de `formacao` nem de `programa`. A O6 é a tarefa 8 e depende
deste pedido — se ele não for atendido, a tela declara a classe como forma nova e local, e diz
que declara.

### PEDIDO-S6-03 · navegação para as dez telas a partir de `/studio/`
`/studio/page.tsx` é da S7 pela fronteira de hoje e **eu não edito**. Sigo com navegação no
cabeçalho da própria superfície, do mesmo jeito que a S5 fez com a Redação. Quando a S7 puder,
o acréscimo na raiz é commit dela.

### PEDIDO-S6-04 · `organizacao.css` fora de `FOLHAS_MIGRADAS`
`scripts/verificar-ds.mjs` é infra compartilhada e **não foi tocada**. A minha folha passa hoje
pelas regras 1 e 4 (0 hex, 0 declaração de token), mas a regra 5 — medida fora da grade de
0,25rem e `text-align` centralizado — **não roda sobre ela** até alguém acrescentar o nome na
lista. Escrevi a folha na disciplina da grade mesmo assim; o pedido é para o gate valer, não
para a folha mudar.

## Divergências entre PRD e código

### DIV-01 · o Studio não tem visão app — herdada da S7, reconferida no código
As seções «**App.**» das dez telas do meu PRD estão **fora de escopo**. Conferido por mim, não
aceito de segunda mão:

- `src/app/(bastidor)/layout.tsx:16` — o bastidor inteiro sob `<div className="app:hidden">`
- `src/estilos/studio.css:9` — *"O Studio só existe na visão web (D-67)"*

Decidido pelo dono do projeto em 25.08 para a S7; vale igual para mim, que estou na mesma pasta
e sob o mesmo layout. As dez telas se escrevem só para a visão web.

### DIV-02 · a §2 do meu PRD manda usar `studio.css` — e isso mudou
O PRD diz *"`src/estilos/studio.css` — que já existe e já está importado. Você não toca no
arquivo de colisão"*. A fronteira de 25.08 22:4x inverteu as duas metades: `studio.css` é da
S7 e eu **não encosto**; a minha folha é `organizacao.css`, que eu crio, e a linha no
`globals.css` é minha e foi commitada sozinha em `4a14e13`.

### DIV-03 · o §9 do protocolo caiu para esta sessão
Não espero confirmação tela a tela. Escrevo o plano aqui, decido e sigo, com o porquê na nota
de cada entrega. **Paro e subo** em dois casos só: mudança de ontologia, ou necessidade de
tocar território proibido pelo §6.

## Plano da tarefa 2 — O2 · Espaços

`(bastidor)/studio/espacos`, a maior conversão de procedência da sessão. O que ela escreve:

1. `src/dados/mock/organizacao.ts` — módulo de **servidor**, DTO de primitivo só. Ele lê os
   113 espaços por `catalogoDoStudio()` e mede, sobre o dado e não sobre literal digitado:
   quantos são `derivado`, quantos declaram acessibilidade, e o denominador das 2.425
   ocorrências sem espaço declarado.
2. `src/componentes/studio-org-estado.ts` — a loja do cliente, chave própria.
3. `src/componentes/studio-org-espacos.tsx` — a tela: lista à esquerda, ficha e o que falta à
   direita, no `.web-duas-colunas` que já existe.
4. `src/app/(bastidor)/studio/espacos/page.tsx` — a página de servidor.

As três regras duras que a tela tem de sustentar: **coordenada continua `derivado`** com
`MetodoCoordenada` registrado — não existe latitude digitada; **ficha do espaço tem ato
explícito** de declarar ausência; e o selo mostra o registro **saindo** de `derivado`.
