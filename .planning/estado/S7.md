# S7 · Studio · Produtor

sessão: bid-ita-63
estado: rodando
tarefa: 6 de 11
pasta: `(bastidor)/studio/`
prd: [`sessoes/S7-STUDIO-PRODUTOR.md`](../sessoes/S7-STUDIO-PRODUTOR.md)

> **Só a S7 escreve neste arquivo.** A sessão de controle lê e agrega no `PAINEL.md`.
> Uma entrega só entra na tabela **com hash de commit**. Sem hash, não aconteceu.

## Entregas

| # | tarefa | commit | quando | nota |
|---|---|---|---|---|
| 1 | contrato, semente e persistência (Fase 0) | `73976b8` | 25.08 20:20 | `Situacao` com 5 estados é o tipo que faltava — sem ele o Studio é formulário. As 4 adições de §12 entraram por **extensão**, sem editar `tipos.ts`: ver PEDIDO-01. Semente determinística de 5 registros a partir de eventos reais; o que é autorado é a situação, não o título. Chave de identidade medida em 2/3 nos rascunhos e 3/3 no devolvido e nos publicados — é essa conversão que a jornada existe para fazer. |
| — | `DecisaoDeModeracao` para a S3 (PEDIDO-03) | `5a59b0f` | 25.08 20:35 | Fora da ordem do PRD, para destravar a S3. Regra que passa a valer: **o que cruza entre níveis mora em `tipos-acesso.ts`; o que só um nível lê mora no módulo daquele nível.** Por isso só `DecisaoDeModeracao` veio; `Delegacao`, `FamiliaDeSimilaridade` e a quarta origem ficam em `moderacao.ts`. Mandei junto `situacaoApos()` e `decisaoCompleta()` — sem elas os dois lados concordam por boa vontade, não por contrato. |
| 2 | P2 · identidade — estende `publicar` | `20f5318` | 25.08 20:55 | A chave fica viva na coluna direita e nasce em **2/3**: título + organização, com o terço da obra visivelmente aberto. Decidi que **o agente realizador é a organização, não campo de formulário** — quem publica é quem realiza, e digitável abriria porta para declarar realização alheia. Carimbo é lista de definição, não campo desabilitado: campo desabilitado promete que um dia se habilita, e procedência nunca se habilita. Catálogo estreito de 65 KB em vez dos 200 KB do inteiro. Verificado em Chrome sem tela: recarregar preserva, reiniciar zera, duas colunas em 1024/1440/1920 sem estouro, zero erro de console. |

| — | `Situacao` ganha `vetado`; `AcaoDeModeracao` vai a seis | `305193a` | 25.08 21:30 | Exceção da S3, aprovada pelo dono. Conferi as três alegações no código antes de mexer: `ACOES_DA_MODERACAO` (`moderacao.ts:987`) tem `Devolver` com `motivo:"opcional"`, a `FRASE_DA_ASSIMETRIA` (`:1026`) é impressa na tela, e a minha própria `Situacao` dizia que `suspenso` é «estava publicado e foi retirado» — o que prova que vetar não cabe ali. Efeitos que vieram junto: `vetado` fora de `SITUACOES_EDITAVEIS`, e a explicação de `devolvido` reescrita porque prometia um motivo que agora pode não existir. |
| — | conserto da P2 achado abrindo a tela | `1e131c8` | 25.08 22:10 | Dois defeitos que passaram limpo nos portões estáticos. **A chave partia a data ao meio** (`2026-` / `07-16T20:00`), o que faz chaves iguais parecerem diferentes — `<wbr>` nos separadores estruturais em vez de proibir a quebra. **A lista de temas cortava em 24 sem dizer**, com 94 no vocabulário: quem procura um termo que existe e não o vê propõe um duplicado pela porta do Editor, ou seja, a tela criando trabalho para a moderação desfazer. Corrigi também o meu próprio diagnóstico: quebra em hífen é tipografia legítima, minha regex de teste é que estava errada. |
| 3 | P5 · grade de ocorrências | `1f18109` | 26.08 00:05 | A tela onde a sessão datada passa a existir. Decidi **teto de 200 sessões por aplicação, declarado na prévia** — uma temporada de 87 dias × 7 dias × 3 horários passa de mil linhas, e limitar a entrada vem antes de otimizar o que veio. Decidi também **aritmética em `Date.UTC` com leitores `getUTC*`**, nunca `new Date(string)`: em fuso brasileiro a grade sairia deslocada em um dia sem nenhum portão ver. Extraí o seletor de registro para `studio-seletor.tsx` — segundo caso de uso chegou. E `.studio-botao` ganhou estado desabilitado, que faltava para as seis sessões. |

| 4 | P3 · obra e elenco | `e1fb5e0` | 26.08 01:10 | **Contei o número da tese errado e quase reportei.** 426 arestas `atua_em` apontam para `evento`, mas **zero para evento datado** — os que a Enciclopédia liga a artistas são históricos, sem sessão; os 129 com ocorrência têm zero artistas. «Evento» e «evento datado» são disjuntos neste acervo. Decidi **ler os papéis das próprias arestas** (3 distintos, «artista» em 312) em vez de escrever lista à mão, e **pôr o papel antes da busca** — escolher a pessoa primeiro convida a vincular sem papel, que é a aresta inválida que a tela impede. Consertei também a classe de combinação da busca, escrita com caracteres literais: NFC a apagaria sem rastro e o `tsc` seguiria verde. |

| — | conserto: coluna colada, data ISO, duas grafias | `40bf9c0` | 26.08 02:40 | Revisão cruzada da S1. **A coluna colada da `publicar` media 1.279px numa janela de 960 — 319px inalcançáveis**, e o que estava lá era a validação ao vivo e o denominador. Reusei `.web-coluna-fixa` de `web.css`, que já descrevia o problema. Corrigi meu próprio teste no meio: «a coluna cabe» passava; «a coluna gruda inteira» falhava por causa do `sticky` sendo empurrado pelo fim da grade, não pelo teto. Passei a medir **«cada painel chega inteiro à vista»**. Achei junto que eu tinha copiado `comSeparador` para três componentes. |
| 5 | P4 · espaço e temporada | `cd54d88` | 26.08 03:57 | Decidi **o espaço antes do intervalo**, na ordem em que a chave se monta — pedir a data primeiro convida a criar temporada sem lugar. Decidi que **remover temporada leva as sessões junto, declarando quantas**: sessão órfã é registro sem chave. Resolvi o `temporadasDe(...)[0]` pendente (0 de 300 têm mais de uma; estava certo por acidente). **Escrevi a classe de combinação com caracteres literais pela segunda vez no mesmo dia** — `simplificar` foi para um lugar só. E separei `<summary>` do botão que executa: os dois tinham as mesmas classes, e minha sonda clicou no gatilho concluindo que a remoção não funcionava, quando o código estava certo. |

| 6 | P6 · ficha de acessibilidade | `07b4817` | 26.08 07:20 | A tela dos **três estados**, e não dois: «declarou e oferece», «declarou e não oferece» e «não declarou». O terceiro é o que some quando uma interface só tem caixas de marcar, e é o único que a plataforma se proibiu de interpretar. Decidi que **marcar qualquer dimensão também declara a ficha** — quem aponta um recurso preencheu —, e que **desmarcar a última NÃO devolve ao silêncio**: a declaração já foi feita, e desfazê-la exige ato próprio, com confirmação. O botão do ato leva a frase inteira e largura total, sem raio de pílula: pílula é forma de controle curto, e esta é uma afirmação. Ficha do espaço em leitura, com 0 campos editáveis medidos. |

**A nota é obrigatória**: uma linha sobre o que foi feito, o que ficou de fora, ou o que
surpreendeu. O hash diz que aconteceu; a nota diz o quê.

### O que a tarefa 1 entrega, em números medidos

```
npm run checar             40 verdes · 1 falha (cursos.css, herdada)
determinismo da semente    true — dois JSON idênticos
registros semeados         5 · 2 rascunho · 1 devolvido · 2 publicado
score dos 5                58% · 67% · 75% · 100% · 100%
chave do evento            2/3 · 2/3 · 3/3 · 3/3 · 3/3
catálogo como DTO          201.751 bytes (792 agentes · 239 obras · 113 espaços · 300 eventos)
```

### O que a S3 herda e não redeclara

| Arquivo | O que traz |
|---|---|
| `src/dados/tipos-acesso.ts` | `Situacao`, `Porta` e as três portas, `RascunhoDoProdutor`, a cadeia de identidade nos três níveis, `scoreDoRascunho`, `pendenciasDoRascunho`, `conversaoDoEnvio`, `CHAVE_DE_ARMAZENAMENTO` |
| `src/dados/mock/seed.ts` | **servidor**: `catalogoDoStudio()` e `rascunhosSemeados()` |
| `src/componentes/studio-estado.ts` | **cliente**: `useStudio()` |

Quatro regras que o contrato impõe a quem o consumir:

1. **`tipos-acesso.ts` pode ser importado por valor no cliente.** Nenhum import de dado sai
   dele — manter assim é requisito de DP-F.
2. **`mock/seed.ts` é módulo de servidor.** Cliente importa **apenas por tipo**.
3. **A chave tem sempre três componentes próprios por nível.** `partesDaChave()` conta, e a
   etiqueta do nível não entra na conta.
4. **Procedência é carimbo, nunca campo.** `PROCEDENCIA_DO_PRODUTOR` é constante.

## Bloqueios

—

## Pedidos de contrato

### PEDIDO-01 · consolidar as adições de §12 em `src/dados/tipos.ts`
`tipos.ts` é contrato de outra sessão e **não foi editado**. As cinco adições que §12 da
ontologia pede entraram por extensão aditiva em `tipos-acesso.ts`:

| §12 pede | O que existe hoje, commitado em `73976b8` |
|---|---|
| `Situacao` | `Situacao` — tipo novo, sem conflito |
| `faixaEtaria` em `Entidade` | `EntidadeClassificada = Entidade & { faixaEtaria?: FaixaEtaria \| null }` |
| `canalIngresso` em `Ocorrencia` | `OcorrenciaDeclarada = Ocorrencia & CamposDeIngresso` |
| `inscricao` em `Ocorrencia` | idem |
| `"produtor"` em `Procedencia` | `ProcedenciaDePapel = Procedencia \| "produtor" \| "parceiro" \| "curador" \| "ia"` |

Nada quebra enquanto as duas formas convivem. **A consolidação é higiene, não desbloqueio**
— a S1 pode ser liberada com o que já está commitado.

### PEDIDO-02 · `scripts/medidas.mjs` não mede as telas da S7
O portão 4 do PRD pede medida de pixel nas telas novas. `medidas.mjs` é arquivo
compartilhado e não foi tocado. As dez telas da S7 ficam fora da medição até alguém as
acrescentar lá.

## Divergências entre PRD e código, levantadas antes de codar

### DIV-01 · D-67 vence o §8.1 do PRD — o Studio não tem visão app
**Decidido pelo dono do projeto em 25.08.** O PRD especifica layout de app nas dez telas; o
código faz o oposto, por decisão verificada:

- `src/app/(bastidor)/layout.tsx:16` — bastidor inteiro sob `<div className="app:hidden">`
- `src/estilos/studio.css:9` — *"O Studio só existe na visão web (D-67)"*
- `04-05-SUMMARY.md:209` — portão verificado: *"conteúdo de bastidor visível: **false**"*

**Consequência:** as seções «App.» das dez telas estão **fora de escopo**. As telas se
escrevem só para a visão web, reusando o vocabulário de `web.css`.

### DIV-02 · `npm run checar` já estava vermelho antes da S7
`cursos.css`, 11 medidas fora da grade, do commit `4da75f5`. Igual ao §5 do protocolo. **O
critério de pronto da S7 é «40 verdes e só a falha herdada».**

## Invasão declarada

**Sobrescrevi `.planning/PAINEL.md`** entre 18:56 e 19:30, antes de o protocolo chegar: o
meu prompt original mandava criar esse arquivo, e ele já existia, commitado em `6bedf92`
como território exclusivo da sessão de controle. **Restaurado ao conteúdo de `6bedf92`**; o
que eu tinha escrito virou este arquivo. Nada se perdeu — estava em commit.

**Matei o `next dev` (PID 80190)** às 19:2x, com aval do dono do projeto e antes de o
protocolo definir servidor por sessão. Ele subiu de novo às 19:28, de outra sessão. Também
rodei `npm run build` por conta própria duas vezes e apaguei `.next` — o §4 do protocolo
proíbe, e eu não o conhecia. **Não repito: daqui em diante peço a vez de build.**

## Notas de execução

### Ordem de confirmação suspensa em 25.08
Ordem do dono do projeto, roteada pela sessão de controle: **seguir sem parar tela a tela**.
As recomendações que eu escrevo passam a valer como decididas por mim, e o porquê fica na
nota de cada entrega. **Duas exceções continuam de pé:** mudança na ontologia ou toque em
território do §6 — nesses dois casos eu paro e subo para a sessão de controle.

### Fronteira com a S6 · Organização
Ela entrou em `(bastidor)/studio/` em paralelo. Minhas rotas, `componentes/studio-*` e
`src/estilos/studio.css` são meus; `studio-org-*` e `organizacao.css` são dela.

**Seis classes de `studio.css` passam a ser interface pública** — a S6 as reusa e renomear
quebraria a tela dela sem erro de compilação: `studio-painel`, `studio-tabela`,
`studio-lados`, `studio-nao-sustenta`, `studio-botao`, `studio-pastilha`. Só acrescento.

**A raiz `/studio/page.tsx` é minha e ainda não existe** — é a P1, tarefa 9. Quando eu a
criar, acrescento a lista de rotas da S6 no mesmo commit.

**Ficha de acessibilidade, carimbo e selo de procedência ainda não são componentes
próprios** — estão embutidos no corpo de cada tela. Extraio quando construir a P6 e a P2,
que é onde preciso deles; extrair antes do segundo caso de uso seria abstrair no escuro.

### `npm run checar` fica vermelho quando outra sessão está no meio de uma edição
`tsc --noEmit` cobre o projeto inteiro, então código em curso da S3 e da S5 derruba o meu
portão sem que nada meu tenha quebrado. Passei a medir também com
`npx tsc --noEmit | grep -E "studio|tipos-acesso|mock/seed"`, que isola o que é meu.
Em 25.08 20:50: nenhum erro nos meus arquivos, com `redacao-trilha.tsx` vermelho.

### `next dev` reescreve o `tsconfig.json`
Rodei `NEXT_SESSAO=s7 npx next dev -p 3007`, como manda o §4. O Next acrescentou sozinho
`.next-s7/types/**` e `.next-s7/dev/types/**` ao `include` do `tsconfig.json`, que é
território da sessão de controle. **Não desfiz** — é aditivo, e o próprio `AGENTS.md` avisa
que desfazer só recria a mudança. Fica declarado.

### Sobre abrir a tela antes de chamá-la de pronta
Três verificações da P5 passaram sem provar nada na primeira rodada: não havia colisão para
o detector achar, não houve truncagem para a declaração exercitar, e eu não tinha instalado
sonda de console. **Verde sobre caso que não aconteceu é verde falso.** Refiz forçando os
três: 261 sessões possíveis contra teto de 200, duas sessões igualadas à mão, e
`cdp.consola` lido de verdade. A colisão foi provada nos dois sentidos — zero no estado
semeado, duas ao igualar, zero de novo ao desfazer.

Achado no caminho: aplicar 200 sessões sobre as 4 semeadas **produziu 4 colisões reais**,
porque a grade regenerou horários que já existiam. O detector pegou sozinho. Foi a asserção
do meu teste que estava errada, não o código.

### Três vezes a verificação estava errada, não o código
Aconteceu na P5 (a colisão que a sonda achou já existia de verdade), na P2 (quebra em hífen é
tipografia legítima; minha regex de teste é que não incluía `-`) e na P4 (a sonda clicou no
`<summary>` e concluiu que a remoção falhava). **Descartar o próprio veredito depois de medir
custa mais do que achar o defeito**, e as três vezes o custo foi menor que reportar errado.

### O padrão que se repetiu três vezes: verde sobre caso que não aconteceu
Na P5 e na P3, **cinco** verificações minhas passaram sem exercitar nada — não havia colisão,
não havia truncagem, a busca devolveu menos que o teto. Toda vez tive de forçar o caso e
rodar de novo. Passou a ser regra desta sessão: **antes de aceitar um verde, conferir se o
caso que ele mede chegou a existir.**
