# S3 · Moderação

sessão: bid-ita-68
estado: rodando
tarefa: 11 de 11 — todas as telas entregues; falta só o build, que depende dos órfãos
pasta: `(bastidor)/moderacao/`
prd: [`sessoes/S3-MODERACAO.md`](../sessoes/S3-MODERACAO.md)

> **Só a S3 escreve neste arquivo.** A sessão de controle lê e agrega no `PAINEL.md`.
> Uma entrega só entra na tabela **com hash de commit**. Sem hash, não aconteceu.

## Entregas

| # | tarefa | commit | quando | nota |
|---|---|---|---|---|
| 1 | separação de território | e7ed486 | 25.08 20:47 | `moderacao.ts` + `moderacao.css` nascem, a tela 34 vira `/moderacao/fila/`, uma linha em `globals.css`. **Libera a S5**: `redacao.ts` e `redacao.css` ficaram só com o editor de trilha e **nenhuma classe CSS é compartilhada** — a fila usa `moderacao-*`. Tocou 9 arquivos fora da pasta, com autorização explícita do dono do projeto e **antes de o PROTOCOLO §6 existir** — ver INVASÃO abaixo. |
| 2 | M1 · a fila | c274111 | 25.08 21:52 | Quarta origem (denúncia, 120), ordem por vazio (124) com o registro da UF na linha para a ordem ser conferível a olho, escopo impresso com o que fica de fora (122), persistência em `moderacao.v1` e reinício com confirmação. **Suíte nova `verificar-moderacao.mjs`, minha, 21 verdes por gesto em navegador — não toca nenhuma `verificar-fase*.mjs`.** DTO 47.894/61.440. Ficou de fora: os registros vivos da S7, declarados em zero com a causa escrita, porque a tarefa 5 está bloqueada. Surpreendeu: pedir a visão «web» é obrigatório em teste de bastidor — sem isso todo clique falha por retângulo zerado. |
| 3 | M2 · ficha do item | 7435e09 | 25.08 23:4x | Ficha campo a campo na ordem da ontologia, chave de identidade componente a componente, barreira de crédito de imagem explicada com dono da correção (114), as duas recusas escritas na tela — classificação se confere e não se arbitra (119), termo se encaminha e não se cria (118). Armazém virou módulo (`moderacao-armazem.ts`) e a falha do navegador virou frase na tela. Migrado para o contrato `305193a`: `DecisaoRegistrada extends DecisaoDeModeracao`, e o veto ganhou a **terceira trava**, `decisaoCompleta()` do contrato. **33 verdes.** Surpreendeu: dois defeitos só apareceram ao OLHAR a tela — botão desabilitado com desenho de ativo e lista dizendo «68 pendentes» mostrando oito. Viraram gate. |
| 4 | M9 · meu histórico | 2e364e1 | 26.08 00:2x | Toda decisão com o destino do OUTRO LADO — vetar é o único sem rota, e diz por quê. Vetos em painel separado com motivo inteiro. Filtro por ação, origem e escopo, e o vazio do filtro declara que nada se perdeu. A tela **diz o que não é**: não compara moderadores — isso é a 169, do Admin. **39 verdes.** Surpreendeu: a suíte media o carimbo por regex dentro de template literal, onde a barra invertida colapsa — ficou vermelho sem causa. Carimbo passou a viajar como atributo nas três telas. Teto de navegação da suíte subiu para 120 s: o dev chegou a 44 s por página com seis sessões na máquina. |
| 5 | M3 · revisão da IA | 799d5c0 | 26.08 00:5x | **O portão central da sessão, provado:** o score exibido É a soma dos cinco componentes marcados — 1×0,2 = conta na tela = score, os três medidos juntos. A lista **não** ordena por confiança, e a tela diz por quê: ordenar por score ensinaria a varrer aprovando. Descartar é vetar e exige motivo. Aprovar deixa registro com origem «ia». **44 verdes.** Ligei as três telas entre si — IA e histórico existiam no artefato e não tinham link nenhum. Surpreendeu: o cabeçalho sticky da fase 4 passou a COBRIR os botões (7 blocos, +300 px); a própria suíte pegou com «ponto central coberto por HEADER.studio-cabecalho», e o que derruba clique automatizado derruba o de uma pessoa. |
| 6 | M8 · escopo e escalonamento | 181d092 | 26.08 01:2x | O escopo imprime as DUAS metades: 19 dentro + 49 fora = 68. 4 escalonamentos com destinatário nomeado — um deles existe só para os 53 sem território. Delegação com início e fim; a encerrada continua listada. **50 verdes.** Achei dois defeitos de raiz: `.web-alternador` **não existe mais** (virou a primitiva `Segmento`) e os botões saíam nus nas 4 telas — herdado da fase 5; e a aritmética de data virou inteiro `AAAAMMDD`. **Testei o portão central com defeito injetado: ficou VERMELHO.** |
| 7 | correções de raiz | b3f0696 | 26.08 01:4x | Data ISO na frase do carimbo → formatada (achado da S1). E `territorioDe()` passou a filtrar relação e direção como `geo.ts` faz: 0 de 68 afetados hoje, mas «certo por acidente do dado» deixa de ser certo quando o acervo cresce. Mesmos 19/15 depois da mudança. |
| 8 | M5 elenco · M6 reconciliação | 6c2b259 | 26.08 02:1x | A barreira ética: a única tela em que a pessoa afetada não está na conversa. Vínculo sem verbete **não se confirma** — criaria a pessoa pela porta dos fundos —, com trava no botão e na função. Reconciliação **liga e nunca edita**, aviso permanente acima da decisão, candidatos por nome normalizado e **zero pontuação de similaridade**. **58 verdes.** Surpreendeu: a grafia variante casa sempre no dado encenado, então o caminho «nenhum destes» existe mas não é exercitado — declarado na tela. |
| 9 | M4 · similaridade | 89957ce · 00e420c | 26.08 02:5x | **47.259 arestas, 71% do grafo, medido por caminho independente e batendo com a ontologia.** Governa a REGRA, não a aresta: 12 famílias por padrão de justificativa, com amostra e método declarados. O contador de **quantas seguem sem revisão fica no topo**, e a cauda (529 famílias, 28.352 arestas) entra nele em vez de sumir. Reprovar milhares de uma vez exige motivo. **63 verdes.** Surpreendeu: `replace(/\D/g)` dentro de template literal colapsa a barra — **três vermelhos falsos**, e passava por acidente com números < 1.000. **Nove ocorrências corrigidas: todo número e data agora viajam como atributo.** E o gate da denúncia ganhou piso por fonte (achado da S1), provado com o atributo renomeado: ficou vermelho. |
| 10 | M7 · duplicatas entre organizações | d081237 | 26.08 03:2x | **O recorte devolve ZERO: nenhum dos 84 grupos junta fontes diferentes** — este acervo não tem uma única duplicata entre organizações. Declarado com os dois denominadores em vez de forçado. Sem botão de decisão nos grupos listados: oferecer um que a moderação não deve apertar desfaria a regra na interface. `autorado` não conta como fonte — sem esse filtro a tela mostraria 12 casos falsos. **67 verdes.** |
| 11 | suíte · prova de ausências | 934cdb2 | 26.08 03:5x | `--provar-ausencias`: 7 defeitos injetados, **7 vistos vermelhos**, árvore restaurada limpa. A primeira execução pegou 4 de 7, e os 3 restantes ensinaram mais: **um gate estava genuinamente quebrado** (padrão em template literal afirmando «sem pontuação» sobre texto com «similaridade: 0,94»); o veto não caiu com UMA trava derrubada porque são três independentes; e uma injeção apontava o gate errado. |

**A nota é obrigatória**: uma linha sobre o que foi feito, o que ficou de fora, ou o que
surpreendeu. O hash diz que aconteceu; a nota diz o quê.

## Invasão declarada — e7ed486, anterior ao PROTOCOLO §6

**Não desfeita, de propósito**, e a decisão precisa da sessão de controle.

O commit é anterior ao PROTOCOLO. §3 do PRD da S3 mandava mover `/redacao/fila/` →
`/moderacao/fila/` na primeira tarefa, e a rota antiga era citada em **7 arquivos fora da
pasta**. Perguntei ao dono do projeto antes de tocar, com as três opções na mesa; ele
escolheu «atualizo links e suítes». Desfazer agora deixa **3 links em 404** e a rota nova
inalcançável pelo menu.

| Arquivo | O que mudou | Reverter custa |
|---|---|---|
| `src/componentes/menu-lateral.tsx:91` | link e rótulo → `Moderação` | menu aponta para rota que não existe |
| `src/dados/apps.ts:335` | idem | atalho de bastidor em 404 |
| `src/app/(bastidor)/observatorio/page.tsx:76` | idem | rodapé do Observatório em 404 |
| `scripts/verificar-fase2.mjs` · `fase3` · `fase4` | string de rota na lista de páginas | suíte mede rota inexistente |
| `scripts/verificar-tema.mjs:82` · `capturar-telas.mjs:43` | string de rota | idem |
| `scripts/verificar-fase5.mjs` | rota, 3 atributos renomeados, 2 arquivos movidos, DTO do catálogo medido em separado | portão de 05-04 vermelho |
| `scripts/verificar-ds.mjs` | `moderacao.css` entrou na lista de folhas migradas | a folha nova escapa do portão |

**Nada do app público foi tocado** — nem `src/app/(app)/`, nem componente sem prefixo de
sessão, nem folha alheia. O polimento de `59f2e26` veio **depois** do meu commit e não
conflitou.

**A partir daqui obedeço §6 e §7 ao pé da letra**: não abro mais `verificar-fase*.mjs`. A
suíte nova da S3 será `scripts/verificar-moderacao.mjs`, que é minha.

## Portões da M1 — o que está verde e o que não fechou

| Portão | Resultado |
|---|---|
| `scripts/verificar-moderacao.mjs` (suíte da S3, por gesto em navegador) | **21 verdes, 0 falha** |
| `npm run checar` | **40 verdes**, só a falha herdada do `cursos.css` |
| `TETO_DO_DTO` | **47.894 / 61.440**, margem 13.546 |
| `npm run build` | **VERMELHO — e não é meu.** 119 páginas do app público estouraram o limite de 60 s por contenção de CPU: 4 `next dev` de outras sessões rodavam durante a vez de build. **Zero erro que não fosse timeout, e nenhuma página da S3 entre as que falharam.** Vez devolvida à coordenação com a proposta de encerrar os devs antes de conceder a próxima. |

A rota `/moderacao/fila/` **já foi provada no artefato estático** na tarefa 1 (e7ed486), com
o contrato de atributos completo: 60 itens, 60 origens, 20 scores, 4 ações, 3 escopos.

## Bloqueios

**Tarefa 5 (integração com o armazém da S7) — bloqueada.** §4 do PRD manda não começar antes
de `tipos-acesso.ts` estar commitado. Ele continua **untracked**:

```
?? src/dados/tipos-acesso.ts
?? src/dados/mock/
?? src/componentes/studio-estado.ts
```

Até lá construo contra a fila do build, que é independente. Destrava: a S7 commitar.

## Pedidos de contrato

### PEDIDO-03 · ATENDIDO em `305193a` (S7) · migrado em `db8610c`
**Era para:** S7, que tem a guarda do arquivo.

§10 do PRD da S3 manda escrevê-los **em `tipos-acesso.ts`, não em `moderacao.ts`**, porque a
S7 também os consome — ela precisa exibir a devolução com o motivo que a moderação escreveu.
O arquivo é dela e está sem commit, então **não o toquei**: sigo com definição local em
`moderacao.ts` e migro quando for atendido.

| Tipo | Campos | Quem lê do outro lado |
|---|---|---|
| `DecisaoDeModeracao` | ação, motivo, autor, carimbo, item, escopo | S7 — a devolução com motivo, na tela do produtor |
| `Delegacao` | quem, qual escopo, início, fim | ninguém fora da S3 (M8) |
| `FamiliaDeSimilaridade` | padrão de motivo, tamanho, método da amostra | ninguém fora da S3 (M4) |
| `OrigemDoItem += "denuncia"` | a quarta origem (funcionalidade 120) | ninguém fora da S3 |

Só o primeiro é de fato compartilhado. Os outros três podem ficar em `moderacao.ts` de vez,
se a S7 preferir manter `tipos-acesso.ts` enxuto — **digam qual das duas.**

### PEDIDO-04 · `scripts/medidas.mjs` não mede as telas da S3
Portão 5 do PRD pede medida de pixel nas telas novas. `medidas.mjs` é compartilhado e, por
§6, não é meu. As 9 telas da S3 não entram na medição até alguém as acrescentar lá.

## Divergências entre PRD e código

### DIV-S3-01 · o `TETO_DO_DTO` media duas telas que nunca viajaram juntas
O portão media `JSON.stringify({ fila, cat: catalogo })` — a fila de moderação **mais** o
catálogo de arrasto do editor de trilha. As duas nunca foram para o mesmo navegador: a fila
serve `/moderacao/fila/`, o catálogo serve `/redacao/trilha/`.

```
medida ANTIGA, hoje (fila + catálogo):   57.020 bytes   ← o PRD declara 56.615
depois do corte:
  /moderacao/fila   39.590 / 61.440   (margem 21.850)
  /redacao/trilha   17.431 / 61.440
```

**O número do PRD está 405 bytes desatualizado** — a margem real antes do corte era 4.420,
não 4.825; o acervo mudou desde a medição. E **a margem da fila é bem maior do que o PRD
supõe**: 21.850 bytes. O teto continua 61.440 e não sobe.

### DIV-S3-02 · as seções «App.» das 9 telas estão fora de escopo
Mesma decisão que a DIV-01 da S7, já tomada pelo dono do projeto: o bastidor inteiro roda
sob `app:hidden` (`src/app/(bastidor)/layout.tsx:16`), com `AvisoDesktop` no lugar. As telas
da S3 se escrevem só para a visão web.

### DIV-S3-03 · `npm run build | tail` devolve `EXIT=0` mesmo quando o Next falha
Duas execuções morreram sem que o pipe acusasse. Quem medir portão de build **redirecione
para arquivo e leia o `EXIT`**. Com um `next dev` disputando CPU as páginas estouram o
limite de 60 s e o build cai; um build completo levou ~50 min.

## Herdado, para quem pegar a Redação (S5)

`/redacao/trilha/` **não tem entrada de menu nenhuma** — não tinha antes da tarefa 1 e
continua sem. Quem chega lá digita a URL. O menu agora diz «Moderação».
