# S2 · Gestor / Observatório

sessão: bid-ita-53 [7ad229]
estado: rodando
tarefa: 6 de 10 — toda tela entregue passa a ser aberta na 3002 antes do commit
pasta: `(bastidor)/observatorio/`
prd: [`sessoes/S2-GESTOR.md`](../sessoes/S2-GESTOR.md)

> **Só a S2 escreve neste arquivo.** A sessão de controle lê e agrega no `PAINEL.md`.
> Uma entrega só entra na tabela **com hash de commit**. Sem hash, não aconteceu.

## Entregas

| # | tarefa | commit | quando | nota |
|---|---|---|---|---|
| 1 | recorte do DTO por tela | 36c5a89 | 25.08 21:55 | `TELAS` com as 8 rotas e a pergunta de cada uma; `aferirDto()` estende às 7 telas novas o teto de 61.440 que só a raiz aferia. Achado da leitura: o portão 05-08 congela a raiz — ver B1 |
| 2 | G1 · superfície navegável | 60030ae | 25.08 23:05 | navegação das 8 telas DENTRO do `<header>` que já existia — faixa nova empurraria o painel e a linha 2874 mede isso; público sobrevive ao recarregamento; botão que reinicia; a frase de D-89 impressa; raiz aferida em 24.585 de 61.440 bytes. **O hash não é meu**: a S6 rodou `git commit` sem pathspec e varreu meus 4 arquivos do índice compartilhado. Nada se perdeu (`git diff HEAD` vazio), mas o porquê ficou fora do histórico — ver B2 |
| 3 | G3 · impacto cultural | 02d306f | 25.08 23:35 | o par de D-90 lado a lado: Joana **0 de 68** adjacentes reais (`sustentado: true`) contra gratuidade **null** (0 de 300 eventos declaram preço). Par escolhido por REGRA — a persona que deu zero —, `null` quando nenhuma der. Cartão de indicador extraído para arquivo próprio: 7 telas exibem indicador, e cartão copiado seriam 7 lugares para achatar D-90. DTO da tela: 7.144 de 61.440 bytes |
| 4 | G5 · procedência | b65d9a5 | 26.08 00:10 | eixo do tempo: 3 valores hoje → 6 em produção, e cada valor novo é um papel humano. `ic` 4.826/14.882, `derivado` 2.937/51.600, `autorado` 47/81 — esta última marcada como a única que **não sobrevive**. As 4 que abrem dizem «ainda não existe no vocabulário», nunca zero: é D-90 aplicado ao vocabulário. Conferência de 3 pontas por extenso, com o que aconteceria se não fechasse. Painel extraído para arquivo próprio, DOM da raiz intacto. DTO: 8.635 de 61.440 |
| — | conserto visual das três telas | fe9d502 | 26.08 00:40 | abri as três na 3002 e achei **4 defeitos que nenhum portão estático vê**. O pior: «o dado não sustenta» saía com 16 px contra 20 px do denominador — o olho lia **300 eventos como o valor**, a inversão exata que esta superfície existe para não cometer, na tela que existe para prová-la. Mais: rótulos truncados, cartões do par com 417 e 539 px (agora 581/581), borda do sem-lastro a 38% ao lado de laranja sólido. **Portão 05-08 medido ao vivo: painel em y=83, base=694 de 960, sem rolar, 6/7/4/14/2** |
| — | G3 · truncagem silenciosa | 68a233e | 26.08 00:55 | a lista de linguagens novas mostrava 12 sob um rótulo que contava o total. A tela SABIA que eram 15 e não contava — versão de interface do zero silencioso. Hoje o teto não morde (4, 8, 9), e por isso valia consertar antes: sem sintoma até o grafo mudar |
| 5 | G6 · ausência declarada | ad8f69b | 26.08 01:10 | oito buracos medidos: 0 de 300 ingresso · 0 de 2.425 espaço · 0 de 129 elenco · 5.108 de 7.810 ficha · 0 de 529 audiodescrição · 0 de 246 coordenada · 1 de 3 chave · 0 de 4 elementos vazios. `nivelQuePreenche` + `projecao` é o que separa plano de trabalho de lista de desculpas. **Achado na tela aberta:** barra cheia em laranja nos seis zeros lia como meta batida — a «barra de 100% gratuito» cometida do outro lado; saiu |

**A nota é obrigatória**: uma linha sobre o que foi feito, o que ficou de fora, ou o que
surpreendeu. O hash diz que aconteceu; a nota diz o quê.

## Bloqueios

**B2 · O índice do git é um só, e `git commit` sem pathspec varre o das outras.**
Medido: `60030ae`, da S6, levou junto `src/estilos/admin.css` (S1) e os meus quatro arquivos
da G1. Nada se perdeu — conferi com `git diff HEAD`, vazio nos quatro —, mas a G1 está
commitada sob mensagem de outra sessão e o porquê das decisões não existe no histórico. Não
reescrevo histórico com seis sessões em `main`. A regra que proponho, já mandada ao controle:
`git commit -F - -- <caminhos>` sempre, nunca `git add` sem commitar no mesmo comando, nunca
`git add` de pasta.

**B3 · O `tsc` do repositório está vermelho por arquivo de outra sessão.**
`redacao-trilha.tsx`, `moderacao-fila.tsx` e `moderacao.ts` — edição em curso e não commitada
da S3 e da S5; `src/dados/redacao.ts` ganhou campos obrigatórios que o componente ainda não
passa. Zero erro nos meus arquivos. **Isso trava a minha vez de build**, que é o que provaria
o painel inteiro na primeira vista com a navegação nova. Destrava quando as duas fecharem.

**B1 · A raiz `/observatorio/` está congelada por portão existente, e o PRD manda mudá-la.**
`scripts/verificar-fase5.mjs` exige, sobre `observatorio/index.html` e só sobre ela:

- linha 2874 — o painel de procedência **inteiro na primeira vista**, topo e base dentro de
  960 px, sem rolar
- linha 1336 — contagem **exata** no artefato: `procedencia-painel` 1 · `procedencia-fatia` 6
  · `indicador` 7 · `publico` 4 · `denominador` 14 · `nao-sustenta` 2
- linha 2981 — os 4 públicos trocam sem mudar a URL, e **o painel sobrevive aos quatro**
- linha 3000 — o mapa de desertos desenhado, com Sergipe e Tocantins nomeados

O PRD §5 manda a raiz virar seletor de público (G1) e leva procedência (G5) e território
(G4) para rotas próprias. Mover qualquer um dos três **quebra o portão**, e o protocolo §6
me proíbe de editar `verificar-fase*.mjs`.

Resolução que proponho, e que espera confirmação do dono: **a raiz não perde nada.** Ela
mantém painel, sete indicadores, quatro públicos e mapa exatamente como estão, e ganha a
navegação para as sete telas novas. G5 e G4 **estendem** em rota própria em vez de mudar de
lugar. Custo: a raiz continua longa. Ganho: portão 05-08 intacto.

**B4 · A vez de build não vale nada enquanto a máquina estiver cheia — mas o portão 05-08
não dependia dela.** Abri as três telas com `next dev` na 3002 e o cliente CDP de
`scripts/navegador.mjs`, e medi o que o build mediria: painel de procedência com topo em 83 e
base em 694 de 960, sem rolar, com 6 fatias, 7 indicadores, 4 públicos, 14 denominadores e 2
não-sustenta. **O portão 05-08 está provado.** O que ainda falta do build é o export estático
completo e a suíte de fase 5 inteira. Segue o registro original:
O build da S3 morreu com `EXIT=1` e 119 páginas estourando 60 s, sem um único erro de
compilação — só CPU. Causa nomeada pela sessão de controle: dois `next dev` órfãos, PIDs
`36274` e `45309`, que não são de nenhuma das seis sessões. O comando de encerrá-los foi
bloqueado pela permissão da sessão de controle, que subiu os PIDs ao dono. **Consequência
para mim: a G1 e a G3 estão entregues sem build verde**, e com elas o portão 05-08 — o
painel de procedência inteiro na primeira vista com a navegação nova — segue afirmado por
leitura do código, não por execução. Sou a próxima da fila quando a máquina esvaziar.

## Pedidos de contrato

**P1 · `Procedencia += parceiro, produtor, ia, curador`** (PRD §7) mora em `src/dados/tipos.ts`,
que não é meu, e todo `Record<Procedencia, …>` de `observatorio.ts` é exaustivo — acrescentar
valor lá quebra este módulo e possivelmente os das outras sessões. Sigo com união local
`ProcedenciaDeProducao` dentro de `observatorio.ts`, só para o eixo do tempo da G5, e a troco
pelo tipo real quando o contrato abrir.

**P2 · G8 — respondido pela S3, e a resposta é melhor que o pedido.** As duas ausências da G8
são de espécies diferentes e não podem ser achatadas numa só, que é a mesma lógica do
`valor: null` contra o `valor: 0`. Tempo de fila **o acervo não sustenta** — `moderacao.ts`,
em `declaracoesDaModeracao()`, já declara que nenhum registro carrega data de submissão, e essa
frase vira a procedência do número. Volume decidido e taxa de veto **existem a partir da M2, no
armazém do cliente** (`moderacao.v1`), do outro lado do DP-F, que uma página de servidor não
alcança. Não vou ler o armazém no cliente: mediria a sessão de quem olha, não o acervo, e viraria
outra tela. `DecisaoDeModeracao` existe em `tipos-acesso.ts` desde o `5a59b0f` da S7, se a G8
precisar do tipo.

**P2 (registro original) · G8 (tarefa 9) não tem dado de origem.** `src/dados/moderacao.ts` (S3) expõe fila, score,
escopos e ações, e **nenhum carimbo de tempo, nenhuma decisão tomada, nenhum contador de veto**
— conferido em `ItemDaFila` (moderacao.ts:271-293) e na lista de exports. Logo, tempo de fila,
volume decidido e taxa de veto saem `sustentado: false` com declaração. O que a G8 mede de
verdade é a composição da fila por território cruzada com `densidadePorUf()` — fila parada
sobre acervo ralo é abandono, e esse número existe hoje. Não bloqueia; muda o que a tela afirma.
